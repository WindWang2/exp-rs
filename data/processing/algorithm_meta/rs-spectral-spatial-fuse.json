{
  "gpu" : false,
  "id" : "rs:spectral_spatial_fuse",
  "input" : "raster",
  "notes" : "Pure window function of the score plane: tile-agnostic, deterministic; invalid (NoData / non-finite) pixels are never fused and never enter a neighbor's aggregate (renormalized, no zero-fill bias). Bit-exact grade. method 'bilateral' range-weights the neighborhood so score edges are preserved instead of smeared.",
  "output" : "raster",
  "tags" : 
  [
    "spectral",
    "detection",
    "fusion"
  ],
  "task" : "target-detection"
}