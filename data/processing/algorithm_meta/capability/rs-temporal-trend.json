{
  "capability": {
    "applicability": {
      "land_cover": [
        "植被",
        "城市",
        "水体"
      ],
      "scenes": [
        "绿化/退化趋势制图",
        "围填海等长期变化速率估计"
      ]
    },
    "band_roles": {},
    "crs": {
      "requires_projected": false,
      "requires_shared_grid": false
    },
    "determinism": {
      "cost_class": "O(tile × scenes)",
      "grade": "bit_exact",
      "large_raster_safe": true,
      "memory_policy": "multipass_streaming",
      "side_effects": false,
      "stochastic": false
    },
    "failure_modes": [
      {
        "code": "NOT_SUPPORTED",
        "when": "有效观测期数过少",
        "remedy": "每个像元至少需要 2 期有效观测，建议 ≥3 期以获得稳定 R²/RMSE"
      }
    ],
    "family": "temporal",
    "io": {
      "inputs": [],
      "outputs": [
        {
          "data_kind": "raster",
          "file_format": "tif",
          "name": "output",
          "required": true
        },
        {
          "data_kind": "integer",
          "name": "sceneCount",
          "required": true
        }
      ],
      "parameters": [
        {
          "default": "true",
          "name": "apply_qa_masking",
          "required": false,
          "type": "boolean"
        },
        {
          "default": "0",
          "name": "band",
          "required": false,
          "type": "integer"
        },
        {
          "enum": [
            "blue",
            "green",
            "red",
            "red_edge",
            "nir",
            "swir1",
            "swir2"
          ],
          "name": "band_role",
          "required": false,
          "type": "enum"
        },
        {
          "name": "collection",
          "required": false,
          "type": "string"
        },
        {
          "default": "keep_all",
          "enum": [
            "keep_all",
            "reject"
          ],
          "name": "duplicate_policy",
          "required": false,
          "type": "enum"
        },
        {
          "name": "output",
          "required": true,
          "type": "string"
        },
        {
          "name": "scenes",
          "required": false,
          "type": "string"
        },
        {
          "default": "256",
          "name": "tile_size",
          "required": false,
          "type": "integer"
        }
      ]
    },
    "limitations": [],
    "modality": [
      "optical"
    ],
    "operator_group": "temporal",
    "prerequisites": [],
    "schema_version": 2,
    "summary": "逐像元时序趋势拟合：普通最小二乘估计斜率/截距并输出 R²/RMSE 佐证拟合质量；显著性检验请用 rs:temporal_sen_trend。",
    "teaching_use": {
      "concepts": [
        "最小二乘趋势",
        "变化速率",
        "拟合优度"
      ],
      "courses": [
        "遥感应用分析"
      ],
      "exercise": "计算 20 年生长季 NDVI 趋势斜率，用 R² 过滤拟合可信区域；需要显著性检验时改用 rs:temporal_sen_trend。"
    }
  },
  "description": "Per-pixel linear trend (ordinary least squares) across a multi-date collection using real acquisition time intervals (days since the collection reference epoch). Outputs slope, intercept, R², valid observation count and RMSE; the regression accumulates online (numerically stable, O(tile) memory independent of date count).",
  "display_name": "Temporal Linear Trend",
  "id": "rs:temporal_trend",
  "input": "",
  "output": "raster",
  "schema_version": 2,
  "tags": [
    "temporal",
    "trend",
    "regression"
  ]
}
