{
  "capability": {
    "applicability": {
      "land_cover": [
        "任意地物"
      ],
      "scenes": [
        "样本检索与相似影像搜索",
        "少样本分类的特征底座"
      ]
    },
    "band_roles": {},
    "crs": {
      "requires_projected": false,
      "requires_shared_grid": false
    },
    "determinism": {
      "grade": "bit_exact",
      "large_raster_safe": true,
      "memory_policy": "streaming",
      "side_effects": false,
      "stochastic": false
    },
    "failure_modes": [
      {
        "code": "MODEL_NOT_READY",
        "when": "嵌入模型未加载",
        "remedy": "先加载嵌入模型"
      }
    ],
    "family": "classification",
    "io": {
      "inputs": [
        {
          "data_kind": "raster",
          "name": "input",
          "required": true
        }
      ],
      "outputs": [
        {
          "data_kind": "string",
          "name": "backend",
          "required": true
        },
        {
          "data_kind": "string",
          "name": "device",
          "required": true
        },
        {
          "data_kind": "integer",
          "name": "embedding_dim",
          "required": true
        },
        {
          "data_kind": "string",
          "name": "mean_vector",
          "required": true
        },
        {
          "data_kind": "string",
          "name": "model",
          "required": true
        },
        {
          "data_kind": "integer",
          "name": "outBands",
          "required": true
        },
        {
          "data_kind": "raster",
          "name": "output",
          "required": true
        },
        {
          "data_kind": "integer",
          "name": "tileSize",
          "required": true
        },
        {
          "data_kind": "integer",
          "name": "tiles",
          "required": true
        }
      ],
      "parameters": [
        {
          "default": "none",
          "enum": [
            "none",
            "mean"
          ],
          "name": "aggregate",
          "required": false,
          "type": "enum"
        },
        {
          "name": "bands",
          "required": false,
          "type": "integer"
        },
        {
          "default": "0",
          "name": "batchCap",
          "required": false,
          "type": "integer"
        },
        {
          "name": "device",
          "required": false,
          "type": "string"
        },
        {
          "name": "model",
          "required": true,
          "type": "string"
        },
        {
          "name": "output",
          "required": true,
          "type": "string"
        },
        {
          "default": "none",
          "enum": [
            "none",
            "hflip",
            "hvflip"
          ],
          "name": "tta",
          "required": false,
          "type": "enum"
        }
      ]
    },
    "limitations": [],
    "modality": [
      "optical",
      "sar"
    ],
    "operator_group": "ml",
    "prerequisites": [
      "需要嵌入模型；输出为非显示用特征数据。"
    ],
    "schema_version": 2,
    "summary": "特征嵌入提取：用预训练模型把影像编码为稠密特征向量/嵌入图，供检索、聚类与少样本分类复用。",
    "teaching_use": {
      "concepts": [
        "特征嵌入",
        "自监督特征"
      ],
      "courses": [
        "深度学习与遥感应用"
      ],
      "exercise": "提取影像块嵌入并检索与目标样区最相似的 10 个区块。"
    }
  },
  "description": "Run an embedding model on a raster; writes the feature stack (aggregate=mean adds a per-scene mean feature vector to the result).",
  "display_name": "Feature Embedding (Model)",
  "gpu": true,
  "id": "rs:embedding",
  "input": "raster",
  "notes": "Writes the model's feature stack as float32 bands (NaN = invalid pixels). aggregate=mean adds a bounded per-scene mean vector to the result payload; the stack itself is never materialized whole.",
  "output": "raster",
  "schema_version": 2,
  "tags": [
    "inference",
    "embedding",
    "deep-learning"
  ],
  "task": "embedding"
}
