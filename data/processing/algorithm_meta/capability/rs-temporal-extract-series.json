{
  "capability": {
    "applicability": {
      "land_cover": [
        "植被",
        "水体",
        "农田"
      ],
      "scenes": [
        "物候曲线提取",
        "样点时序采样"
      ]
    },
    "band_roles": {},
    "crs": {
      "requires_projected": false,
      "requires_shared_grid": false
    },
    "determinism": {
      "cost_class": "O(roiPixels × scenes) for polygons, O(scenes) for points",
      "grade": "bit_exact",
      "large_raster_safe": true,
      "memory_policy": "streaming",
      "side_effects": false,
      "stochastic": false
    },
    "failure_modes": [
      {
        "code": "DATASET_NOT_FOUND",
        "when": "时序集合中引用的影像缺失",
        "remedy": "检查时序集合清单与文件路径"
      }
    ],
    "family": "temporal",
    "io": {
      "inputs": [],
      "outputs": [
        {
          "data_kind": "table",
          "file_format": "csv",
          "name": "output",
          "required": true
        },
        {
          "data_kind": "string",
          "name": "series",
          "required": true
        }
      ],
      "parameters": [
        {
          "default": "true",
          "name": "apply_qa_masking",
          "required": false,
          "type": "boolean"
        },
        {
          "default": "0",
          "name": "band",
          "required": false,
          "type": "integer"
        },
        {
          "enum": [
            "blue",
            "green",
            "red",
            "red_edge",
            "nir",
            "swir1",
            "swir2"
          ],
          "name": "band_role",
          "required": false,
          "type": "enum"
        },
        {
          "name": "collection",
          "required": false,
          "type": "string"
        },
        {
          "default": "keep_all",
          "enum": [
            "keep_all",
            "reject"
          ],
          "name": "duplicate_policy",
          "required": false,
          "type": "enum"
        },
        {
          "name": "output",
          "required": true,
          "type": "string"
        },
        {
          "name": "point",
          "required": false,
          "type": "string"
        },
        {
          "name": "polygon",
          "required": false,
          "type": "string"
        },
        {
          "name": "scenes",
          "required": false,
          "type": "string"
        }
      ]
    },
    "limitations": [],
    "modality": [
      "optical"
    ],
    "operator_group": "temporal",
    "prerequisites": [],
    "schema_version": 2,
    "summary": "从影像集合中按 ROI 或像元抽取时序曲线，输出点/区时序表，用于时序建模输入与教学演示。",
    "teaching_use": {
      "concepts": [
        "时序曲线",
        "物候"
      ],
      "courses": [
        "遥感应用分析"
      ],
      "exercise": "提取农田样区 NDVI 时序曲线并标注播种与收获窗口。"
    }
  },
  "description": "Extract a time series at a point or inside a polygon ROI from a multi-date collection. Points return (time, value, valid); ROI pixels are bounded by the polygon bounding box and summarized per date (mean/median/min/max/stddev/valid_count). Output: CSV plus the JSON series; missing observations stay missing (no interpolation).",
  "display_name": "Extract Temporal Series",
  "id": "rs:temporal_extract_series",
  "input": "",
  "output": "",
  "schema_version": 2,
  "tags": [
    "temporal",
    "time-series",
    "zonal-stats"
  ]
}
