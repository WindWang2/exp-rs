{
  "capability": {
    "applicability": {
      "land_cover": [
        "植被",
        "水体",
        "农田"
      ],
      "scenes": [
        "耕地撂荒预警",
        "裸土开发行为监测"
      ]
    },
    "band_roles": {},
    "crs": {
      "requires_projected": false,
      "requires_shared_grid": false
    },
    "determinism": {
      "grade": "bit_exact",
      "large_raster_safe": true,
      "memory_policy": "streaming",
      "side_effects": false,
      "stochastic": false
    },
    "failure_modes": [
      {
        "code": "INVALID_PARAMETER",
        "when": "监测窗口或阈值配置缺失",
        "remedy": "补全 window/threshold 监测参数"
      }
    ],
    "family": "temporal",
    "io": {
      "inputs": [],
      "outputs": [
        {
          "data_kind": "string",
          "name": "method",
          "required": true
        },
        {
          "data_kind": "raster",
          "name": "output",
          "required": true
        },
        {
          "data_kind": "integer",
          "name": "sceneCount",
          "required": true
        }
      ],
      "parameters": [
        {
          "default": "true",
          "name": "apply_qa_masking",
          "required": false,
          "type": "boolean"
        },
        {
          "default": "0",
          "name": "band",
          "required": false,
          "type": "integer"
        },
        {
          "name": "band_role",
          "required": false,
          "type": "string"
        },
        {
          "name": "collection",
          "required": true,
          "type": "string"
        },
        {
          "default": "0",
          "name": "drift",
          "required": false,
          "type": "numeric"
        },
        {
          "default": "0.300000",
          "name": "lambda",
          "required": false,
          "type": "numeric"
        },
        {
          "default": "200000000",
          "name": "max_pairwork",
          "required": false,
          "type": "integer"
        },
        {
          "default": "cusum",
          "enum": [
            "cusum",
            "ewma",
            "seasonal_mk"
          ],
          "name": "method",
          "required": true,
          "type": "enum"
        },
        {
          "default": "3",
          "name": "min_observations",
          "required": false,
          "type": "integer"
        },
        {
          "name": "output",
          "required": true,
          "type": "string"
        },
        {
          "default": "256",
          "name": "tile_size",
          "required": false,
          "type": "integer"
        }
      ]
    },
    "limitations": [
      "seasonal_mk is O(sum n_m^2) pairs per pixel; the max_pairwork guard refuses unbounded collections instead of degrading.",
      "argmax bands are 0-based scene indices; scene dates travel in the collection metadata."
    ],
    "modality": [
      "optical"
    ],
    "operator_group": "temporal",
    "prerequisites": [
      "Common grid, acquisition times, consistent radiometric state (temporal preflight)."
    ],
    "schema_version": 2,
    "summary": "时序监测任务算子：以滑动窗口对最新观测做状态评估（继续/警戒/告警），面向业务化持续监测场景。",
    "teaching_use": {
      "concepts": [
        "滑动窗口",
        "状态监测",
        "阈值告警"
      ],
      "courses": [
        "遥感应用分析"
      ],
      "exercise": "配置 NDVI 季度滑动监测并在掉产 20% 时输出警戒状态。"
    }
  },
  "description": "Per-pixel temporal monitoring: CUSUM and EWMA of standardized anomalies, or seasonal Mann-Kendall trend (calendar-month seasons).",
  "display_name": "Temporal Monitor",
  "gpu": false,
  "id": "rs:temporal_monitor",
  "input": "",
  "notes": "CUSUM/EWMA standardize against the full-series per-pixel statistics (retrospective monitoring). Seasonal MK uses calendar-month seasons with the tie-corrected variance.",
  "output": "raster",
  "schema_version": 2,
  "tags": [
    "temporal",
    "cusum",
    "ewma",
    "mann-kendall"
  ],
  "task": "temporal-monitoring"
}
