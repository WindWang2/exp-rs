{
 "gpu": false,
 "id": "rs:temporal_extract_regions",
 "input": "multi-date raster collection + regions array (points/polygons with ids)",
 "notes": "Batch multi-ROI temporal extraction: per region and date reports mean/min/max/stddev/median/valid_count as CSV rows. Streams by date so each scene is read exactly once; memory O(regions).",
 "output": "table",
 "tags": [
  "temporal",
  "zonal",
  "time-series"
 ],
 "task": "temporal-extraction"
}
