# Original User Request

## 2026-08-31T16:01:19Z

全面优化 RS Studio (exp-rs) Qt 6 桌面端的 UI 视觉设计与人机交互体验：以“美观、整齐、人性化”为核心目标，系统性重构工具箱内全部遥感处理对话框的布局对齐与表单结构，并规整主界面各功能区及停靠面板的视觉层次。

Working directory: /home/kevin/projects/rs-studio/main
Integrity mode: development

## Requirements

### R1. 工具箱全部遥感处理对话框网格对齐与排版标准化
- 针对工具箱内全部处理对话框（涵盖波段运算、大气校正、辐射定标、正射校正、图像融合、影像镶嵌、变化检测、主成分分析、高光谱/光谱指数、空间/斑点滤波、地形分析、分类后处理、批量处理、STAC 检索、波段提取等 20+ 个对话框及基类）：
  - 建立统一的栅格/表单排版规范（采用标准的标签对齐、行间距 6~10px、边距 10~14px）。
  - 结构化参数分组：统一使用带清晰边框和标题的 `QGroupBox` 模块化组织输入源、算法参数、输出配置和高级选项。
  - 标准化控件尺寸与比例：文件浏览输入框与选择按钮按统一比例排布，数值调节框与下拉列表高度一致。
  - 统一底部按钮栏：全量采用标准化 `QDialogButtonBox`（或统一顺序的“确定/运行”、“取消”、“重置”、“帮助”按钮），确保布局贴合底部右侧或标准对话框规范。

### R2. 交互易用性与人性化（Human-Centric UX）强化
- 为所有参数输入控件补充精确的 Tooltip 提示、占位文本（Placeholder）及合理的默认预设值。
- 复用并整合项目内成熟的专业组件（如 `RasterLayerCombo`、`BandRoleCombo`、`QgsMessageBar` 等），提供一致的图层/波段选取引导。
- 增强即时输入校验与错误反馈机制（必填项缺失、数值越界时的提示信息与高亮反馈）。
- 确保所有对话框设置合理的 `minimumSizeHint`，消除控件挤压、文字截断现象，并自适应 High-DPI 屏幕缩放。

### R3. 主界面全局视觉层次与停靠面板规整
- 优化主界面主工具栏/Ribbon 功能区、画布工具条、图层树（Layer Tree）、任务中心面板（Task Center）、数据管理器面板（Data Manager）、日志面板（Log Panel）等区域的内边距、分割条（QSplitter）视觉与对齐协调度。
- 统一界面状态反馈样式与空状态展示（Empty States），保证整体视觉沉浸、清爽与专业度。

### R4. 架构一致性与全量构建/测试安全网
- 严格遵循现代 C++20 / Qt 6 规范与项目既有设计原则，不破坏既有数据上下文（`ProjectContext`）、算法引擎与后台异步任务调度（Job Engine）管道。
- 保证每个重构的对话框均有对应的生命周期、信号槽连接及 UI 构造测试覆盖。

## Acceptance Criteria

### 对话框视觉与布局对齐
- [ ] 工具箱所有独立处理对话框（20+ 个功能工具及通用基类）排版整齐，所有表单标签、输入框、浏览按钮完美对齐，无错位或截断。
- [ ] 模块分组框（QGroupBox）和底部操作栏风格在所有对话框间 100% 保持一致。

### 人性化交互与易用性
- [ ] 所有参数项均具备清晰的 Tooltip 说明与合法范围校验。
- [ ] 关键耗时或需要提示的工具对话框均具备规范的提示或进度接入准备。

### 自动化验证与构建测试
- [ ] CMake 编译构建 100% 成功（零错误，无新增编译警告）。
- [ ] Catch2 单元测试套件（包括 UI 对话框与测试用例）全量 100% 通过（Green）。
