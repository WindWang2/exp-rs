<!-- AUTO-GENERATED by scripts/gen_lab_docs.py from data/labs/ — do not hand-edit. -->


# 实验：实验11：制图出图——专题数据生产与合规地图排版（Cartographic Mapping）

- **实验编号**：`lab14_cartographic_mapping`

## 实验目标

Course lab 11 — Cartographic Mapping. Structured teaching content (objectives, principles, glossary, expected artifacts, thinking questions) is carried in the LabSpec 2 fields merged from lab11_cartographic_mapping.labspec.json.

主题：cartography（受众：遥感导论本科生（已完成实验8），约 120 分钟）
理解'分析结果 → 制图产品'的完整链路：专题栅格生产的可复现管道与地图排版的声明式分离
掌握时序数据的合成制图：把 12 期 NDVI 压缩为'年内均值'单幅专题图层
掌握阈值法专题分级（Otsu 自动阈值）并解释其在制图语境下的含义（类别划分 ≠ 数据分析）
掌握专题图合规五要素：标题、图例、比例尺、指北针、来源注记，能用 MapSpec 声明式文档表达它们
理解制图交付治理：output 块只声明交付面、导出必须是显式动作（cartography:export）

## 基本原理

### 从分析结果到专题图层

分析产物（时序、掩膜、丰度）要成为地图，需要一次'制图抽象'：选一个主题变量（本实验：年内 NDVI 均值 = 全年平均光合能力），再把它离散化为读者可理解的类别（本实验：Otsu 阈值把均值的直方图切成植被/非植被两类）。注意方向性：分析时阈值是'假设'，制图时阈值是'表达选择'——同一份数据允许多种分级方案，但图上必须注明分级方法。

- 公式：`年内均值：x̄ = (1/N)·Σ x_t`

- 公式：`Otsu：σ_B²(t) = ω_0(t)ω_1(t)[μ_0(t) − μ_1(t)]² 取最大`

### 专题图合规五要素

一张可交付的专题图至少要能回答五个问题：图叫什么（主标题）、颜色/符号代表什么（图例）、多远是一公里（比例尺）、哪边是北（指北针）、数据从哪来（来源注记，含坐标系 EPSG:4326 与时间基准）。preflight 把这五要素编码为规则（MAP_MISSING_TITLE/LEGEND/SCALE_BAR/NORTH_ARROW/SOURCE_NOTE），缺失会被逐条点名——这就是'合规'的机器可查形式。

### MapSpec：把排版写成文档

MapSpec 是版本化的声明式制图文档（当前 spec_version 5；文档模型含 map_frames/legends/scale_bars/north_arrows/titles/labels/source_notes/constraints/output 等集合，每项带稳定 id 与可选 semantic_role，几何用页面毫米 rect_mm）。MapSpecCompiler 把它翻译成 QGIS 打印布局并渲染。声明式排版的好处：地图可评审、可 diff、可复现——改文字不改几何，改几何不动数据。

### 交付治理：声明 vs 导出

spec_version 5 起文档可携带 output 块（formats/dpi/dir）声明交付面，但编译绝不自动导出——导出必须显式调用 cartography:export（PNG/PDF/SVG，72–1200 dpi，原子写出 + SHA-256）。这把'生成'与'交付'分成两个可审计的动作：导出前 preflight 与 validate 必须先通过，导出后有字体替换诊断可查。

## 实验数据

| 数据 | 说明 |
|------|------|
| `data/labs/data-specs/lab11_cartographic_mapping.json` | 离线数据规格（D1 foundry 生成）；实验室环境见 data-specs 的 spec_ref 字段。 |

## 实验步骤

## 预期成果

| 产物 | 类型 | 说明 |
|------|------|------|
| `data/labs/_tmp/out/lab11/ndvi_composite_mean.tif` | 栅格 | 合成 NDVI：林地 0.65–0.85、水体 -0.20–0.00、扰动地块被采伐拉低；见判分意图 K1 |
| `data/labs/_tmp/out/lab11/thematic_mask.tif` | 栅格 | 专题掩膜 UInt8 {0,1}，有植被覆盖占比 ∈ [80%, 95%]（Otsu 分开水体/低值与植被覆盖）；见判分意图 K2 |
| `rs:threshold_raster 步骤统计` | 文件 | Otsu 阈值 ∈ [-0.05, 0.5] 且 masked 比例自洽；见判分意图 K3 |
| `data/labs/mapspecs/lab11_thematic_map.mapspec.json` | 文件 | MapSpec 通过 validate（零问题）与 preflight（五要素零缺失）；见判分意图 K4 |
| `data/labs/_tmp/out/lab11/map/` | 文件 | 导出 PNG 为 A4 横向 @200dpi，非空；见判分意图 K5 |

## 思考题

1. 同一份年内均值 NDVI，固定阈值 0.45 与 Otsu 自动阈值会给出什么不同的'植被'范围？哪种更可复现？哪种更可跨区比较？
提示：数据驱动 vs 先验驱动；跨区比较时 Otsu 阈值会随直方图漂移
1. EPSG:4326 经纬度网格下比例尺怎么画？为什么高纬度地区'1 厘米 = X 公里'的注记会失真？
提示：度不是等长单位；子午线与纬线收敛
1. MapSpec 里删掉 source_notes 集合，preflight 会报什么？为什么'数据从哪来'是合规要素而不是可选项？
提示：MAP_MISSING_SOURCE_NOTE；可追溯性
1. v5 为什么规定'output 块只声明、编译不自动导出'？把'生成'与'交付'分开能防住什么事故？
提示：未过 preflight 的图被批量导出 / 交付物不可审计
1. 把 map_frames[].layers 改成一个不存在的图层名，编译/导出在哪一步失败？这类'引用完整性'为什么要在校验而非渲染时发现？
提示：fail-fast；错误定位成本

## 术语表

| 术语 | 英文 | 定义 |
|------|------|------|
| 专题图 | thematic map | 以单一主题变量（如植被长势等级）为主体的地图产品，与普通影像图相对 |
| 合成 | composite | 把多期观测压缩为单幅代表值（均值/最优像元/中位数）的制图预处理 |
| MapSpec | MapSpec | 平台版本化声明式制图文档（当前 spec_version 5），由 MapSpecCompiler 翻译为 QGIS 打印布局 |
| 合规五要素 | cartographic compliance set | 标题、图例、比例尺、指北针、来源注记；平台 preflight 以 MAP_MISSING_* 规则逐条检查 |

## 判分规则

本实验的判分规则：`data/labs/grading/cartographic_mapping.rules.json`（`lab --lab <id> --grade` 读取）。

## 自动判分

本实验的自动判分管线：`data/labs/pipelines/lab11_cartographic_mapping.pipeline.json`。
