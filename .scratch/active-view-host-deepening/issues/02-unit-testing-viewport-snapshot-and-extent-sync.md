# 02 — Unit Testing Viewport Snapshot & Extent Sync

**What to build:** Add unit tests in `tests/test_active_view_host_viewport.cpp` verifying `viewportSnapshot()` and canvas extent setter methods on `ActiveViewHost`.

**Blocked by:** 01 — Viewport Snapshot API & Extent Encapsulation in ActiveViewHost

**Status:** ready-for-agent

- [ ] Unit test verifies `viewportSnapshot()` returns valid extent, scale, CRS, and layer name
- [ ] 100% test suite passage across `test_active_view_host_viewport` and repository
