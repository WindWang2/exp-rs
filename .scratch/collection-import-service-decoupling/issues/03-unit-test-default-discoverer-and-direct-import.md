# 03 — Unit Test Default Discoverer & Direct Import Execution

**What to build:** Add unit tests verifying default constructor `CollectionImportService( dataManager )` and direct `importCollection` execution.

**Blocked by:** 01 — Optional ProductDiscoverer in CollectionImportService

**Status:** ready-for-agent

- [ ] Unit test verifies `CollectionImportService( dataManager )` probing with default discoverer
- [ ] Unit test verifies atomic collection commit with default discoverer
- [ ] 100% test suite passage across `test_collection_import_service` and full repository test suite
