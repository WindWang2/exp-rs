# 02 — Process Pool Lifecycle Unit Tests

**What to build:** Add unit test coverage in `test_python_plugin_host.cpp` verifying `poolSize()` and `poolHealth()` functionality.

**Blocked by:** 01 — Dynamic Pool Size Query API in PythonWorkerProcessPool

**Status:** ready-for-agent

- [ ] Unit test verifies `poolSize()` matches constructor parameter
- [ ] 100% test suite passage across `test_python_plugin_host` and repository
