# 01 — Dynamic Pool Size Query API in PythonWorkerProcessPool

**What to build:** Add `int poolSize() const` to `PythonWorkerProcessPool` returning configured worker pool capacity.

**Blocked by:** None — can start immediately.

**Status:** ready-for-agent

- [ ] `int poolSize() const` added to `PythonWorkerProcessPool`
- [ ] Code builds cleanly without warnings
- [ ] Existing python pool tests pass cleanly
