# 03 — ImageEnhancement::piecewiseLinearStretch Backend & GeoTIFF Export

**What to build:** Implement `ImageEnhancement::piecewiseLinearStretch` backend logic and wire into `ContrastStretchDialog` and `TaskCenter` for GeoTIFF export.

**Blocked by:** 02 — Piecewise Linear Control Point Handles in HistogramWidget

**Status:** ready-for-agent

- [ ] Implement `ImageEnhancement::piecewiseLinearStretch` in `src/processing/algorithms/image_enhancement.{h,cpp}`
- [ ] Wire piecewise stretch into `ContrastStretchDialog` and `TaskCenter`
- [ ] Add unit tests in `tests/`
