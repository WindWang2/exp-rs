/***************************************************************************
 * rs_progress_callback.cpp  —  Progress callback implementations
 ***************************************************************************/
#include "rs_progress_callback.h"

namespace sicnu::operators {

// Currently header-only: concrete callbacks live in rs_operator_context.cpp
// This translation unit exists to satisfy the build manifest and to host
// future reusable callback implementations (e.g., file logging).

} // namespace sicnu::operators
