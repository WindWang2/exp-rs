// rs_roi_tool_base.cpp — translation unit ensuring AUTOMOC picks up
// RsRoiToolBase's Q_OBJECT (header-only Q_OBJECT classes need at least one
// .cpp in the build so moc generates the metaobject).
#include "rs_roi_tool_base.h"
