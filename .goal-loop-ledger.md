# .goal-loop-ledger — 每轮记账

格式：`Round N | 当前 Oracle 差距 | 单一聚焦改动 | 验证命令 | 实际结果/exit | PASS/FAIL | 下一步`

| Round | Oracle 差距 | 聚焦改动 | 验证命令 | 结果/exit | 判定 | 下一步 |
|---|---|---|---|---|---|---|
| 0 | 全部（Phase 0 审计） | 刷新 master/PR/issue 事实 + 三路代码审计 + worktree/artifacts | git/gh 只读命令组 | 全部 exit 0 | PASS | Phase 1 权威收敛 |
| 1 | Oracle 1/3（authority、fail-closed、上界） | WP-A/B: consumer-abort fail-closed、fused_chain 取消桥、TileRun 合约+错误信封 | （编译验证 pending） | pending | PENDING | 编译+测试 |
| 2 | Oracle 2（crash/resume） | WP-C: ResumableTileRun+journal+marker+子进程崩溃测试 | （编译验证 pending） | pending | PENDING | 编译+测试 |
| 3 | Oracle 3（资源上界） | WP-D/F: planner (2S+2)I 上界+Governor+遥测采样 | （编译验证 pending） | pending | PENDING | 编译+测试 |
| 4 | WP-E/G/H | WorkerLeaseTracker+pool 接线、adoption kit、规模/故障矩阵 | （编译验证 pending） | pending | PENDING | 编译+测试 |
| 5 | Round1 review P0(:-in-key)/P1(空 journal 头) | identity key 分隔符 `-`、空文件补头、truncation fail-closed、fsync 父目录、consume 移出 catch | 源码 diff review（reviewer 实测 Win32 CreateDirectory 行为） | 修复完成，待编译 | PENDING | 编译+测试 |
| 6 | Round2 review P1(上界 2I/stage)、P1(getpid) | in-hand=(2S+2)I+模拟器 build 态；selfPid ifdef；pipeline cancel 桥+测试；oracle 加强 | census/regex 复核 36 文件不变 | 修复完成，待编译 | PENDING | 编译+测试 |
| 7 | 编译验证 + 首轮全测试 | 修复 4 处编译错误（JobExecutor 签名、k 捕获、_getpid、wipeState const）+ 3 处测试缺陷 + driver state 目录创建 + DiskTileStore write 作用域（既有 Windows 缺陷，纯 STD 对照证明） | 9 新套件 + 5 回归套件 | 全 exit 0 | PASS | Phase 8 |
| 8 | Oracle 7 双验证 | 14 套件连续两遍 | verify-all.cmd ×2 | 两遍全 PASS | PASS | drift 闭环 |
| 9 | Oracle 6 生成物/drift | 新错误码 curated page 闭环；其余失败对照为 master pre-existing | test_diagnostics_contract_9 / git 对照 | operator 家族 0 残留 | PASS | push+PR |
| 10 | Oracle 8 终态 | push + PR #1009 创建（不 merge，不等 CI） | gh pr view 1009 | OPEN/MERGEABLE | PASS | track 完成，等待 reviewer |
# GOAL LOOP LEDGER — cn-eo-product-physics-11

格式：Round N | 当前 Oracle 差距 | 单一聚焦改动 | 验证命令 | 实际结果/exit | PASS/FAIL | 下一步

Round 1 | Oracle 全部未开始（代码未写）| Phase 0 审计+worktree+规划工件落盘 | git rev-parse / gh pr list / worktree add | 基线 a5b11b7f 落定, planning 11 文件+ledger 落盘 | PASS | Phase 1: registry schema 2.0
Round 2 | Phase 1 代码完成未验证 | registry schema 2.0 严格验证+validator+band_axis+drift test 落码 | ninja -j2 sicnu_geospatial test_sensor_schema ... | 构建进行中(冷构建) | PENDING | 构建→targeted tests→commit
Round 3 | Phase 2/3 代码完成未验证 | GF-3/4/5 + ZY-1 02B/02D/02E + CBERS 识别+parser+registry+tests 落码 | 同上 + test_cn_product_families11 | 同上 | PENDING | 同上
Round 4 | Phase 4 代码完成未验证 | dryRunCnProductImport+取消零半成品修复+SAR stamps+CLI/agent/GUI parity | 同上 wave2 (sicnu_operators/agent/cli) + test_product_import_plan11 | 同上 | PENDING | 构建→tests→commits


Round 5 | Wave1 快反馈缺失 | 停全量wave→先链接 test_sensor_schema 拿 Phase 1 验证 | ninja -j2 test_sensor_schema | 修3处(INFO-in-catch/raw-string/using)后 98 assertions 7 cases ALL PASS; commit a8cffb80 | PASS | Wave2: 其余 targets(qgis_core 依赖链)
Round 6 | Phase 1 fixture-harness 环境语义修复 | SICNU_DATA_DIR=root 语义对齐 loader | 同上 | ALL GREEN | PASS | wave2 build/test

Round 7 | Phase 2-5 构建验证 | 修复 qputenv/fixtures测试/dry-run遮蔽/QString转换 后全量构建 | ninja -j2 7 targets | families11 768/9 PASS, fixtures 282/3 PASS, plan11 220/7 PASS(修3处: budget断言/取消句柄泄漏satellite_products修复/readFileText中文路径), cn_products 1378/29 PASS(契约更新), io_products 38/4, io_product_registry 46/8, satellite 479/20 PASS(PROJ_DATA=host env,对照证明与diff无关) | PASS | commits 874c6800 + 4bd37616
Round 8 | dialog suite 未构建完 | 等待 wave3(qgis_gui链) → 跑 test_product_import_dialog | ninja | PENDING | Phase 6 双验证
Round 9 | Review P0/P1 清零 | R-01..R-14 全处置(对话框裸换行 P0/严格JSON类型/校准取消半成品/PAN10规格范围等) | 9套件重建重跑 | 全绿(4011断言) | PASS | commit faddf0b2
Round 10 | Oracle #5/#6 终验 | rebase(无新提交)+diff-check/冲突/secret扫描+9套件连续两遍 | 两遍全绿 | PASS | push + PR
Round 11 | 终态 | push + PR 创建(未 merge,不等待 CI) | gh pr create | https://github.com/WindWang2/exp-rs/pull/1012 | DONE | Oracle: #1 registry 来源+不静默降级 ✅ #2 物理量来源/单位/provenance(无伪造) ✅ #3 零半成品+只读/中文路径/取消 ✅ #4 两遍全绿+schema drift ✅ #5 diff-check/冲突/secret/生成物 clean ✅ #6 两遍连续验证 ✅ #7 review P0=0 P1=0+全 disposition+PR 已建未 merge ✅
# GOAL Loop Ledger — radiometric-physics-11

Format: `Round N | 当前 Oracle 差距 | 单一聚焦改动 | 验证命令 | 实际结果/exit | PASS/FAIL | 下一步`

Round 1 | Oracle 无着落点（无代码） | Phase 0 审计 + worktree + 规划工件 | git rev-parse / gh pr list / cmake --preset | exit 0 | PASS | Phase 1: solar_geometry 模块 + 独立 oracle 测试
Round 2 | Oracle#3 未运行（模块无测试） | solar_geometry 模块 + 5 套独立 oracle 测试 | build + ctest | 首跑 14F+2S | FAIL→修复 | transition/provider 模块
Round 3 | Oracle#2 拒绝语义与多边链 | transition authority + provider seam + 测试 | cmake build | exit 0 | PASS | brdf/qa 模块
Round 4 | Oracle#1 状态可追溯无 operator | 三个流式 operator + 注册 + docs + E2E | build | link error(uint16 writeTile)→writeTileRaw 修复 | PASS | subagent#1 审计
Round 5 | P0 Li-Sparse-R 项错误 + P1×3 | 核公式/文档/测试契约修复 | ctest 49 例 | 49/49×2 | PASS | subagent#2 终审
Round 6 | P2×2 文档/哨兵漂移 | doc/API 更名 + sentinel NaN + P3×5 加固 | rebuild + ctest 49/49×2 + 回归 102 例 | 全绿 | PASS | push + PR
# .goal-loop-ledger — spectral-intelligence-11

格式：`Round N | 当前 Oracle 差距 | 单一聚焦改动 | 验证命令 | 实际结果/exit | PASS/FAIL | 下一步`

- R0 | Oracle 全部未开始 | Phase 0 审计 + worktree + planning 落盘 | git rev-parse; gh pr/issue list | exit 0 | PASS | Phase 1 契约层
- R1 | 契约层缺失 | hybrid/local-RX/sparse/endmember 四内核 + 头文件（新文件避让 #1008） | 文件落盘 | 4+4 文件 | PASS | 测试
- R2 | 无独立 oracle | 4 个内核测试 + scale 测试（闭式/参考实现/determinism） | ctest（构建后） | 待构建 | PART | 构建
- R3 | 无本地构建 | Windows 工具链探测：vcvars64+Qt Ninja+vcpkg manifest+winflexbison+本地 catch2 | cmake -B build-dev -G Ninja ... | 5 轮迭代（preset 退化 VS 生成器→工具链缺失→Qt 前缀→GDAL→BISON→catch2 网络）→ run6 进行中 | PART | build
- R4 | 算子 surface 缺失 | 4 算子 + 双注册点 + operators CMake + capability JSON | 构建 + export-catalog + drift gate | 文件就绪，构建待验证 | PART | build/test
- R5 | 内核未验证 | 修复 scale 测试 Config 歧义 + 本地 catch2 + vcpkg/Qt/BISON 工具链配置 | build1-5 + sic11-test.cmd | sparse PASS / localrx PASS / scale PASS；hybrid+endmember+pipeline+widget 待链接 | PART | 补建+全跑
- R6 | pre-existing Windows 编译缺陷 | pipeline_run_coordinator.cpp 补 fcntl.h/io.h（Q_OS_WIN 守卫，最小修复，记录 OUT_OF_SCOPE） | build4/5 日志 | 修复生效验证中 | PART | CLI 目标
- R7 | 6/7 套件未跑通 | 修 QSignalSpy→手动连接、missing include、autogen 重建 | sic11-test.cmd | hybrid/endmember/pipeline_11/workbench 全 PASS；累计 7/7 套件 exit 0 | PASS | CLI+catalog
- R8 | capability 生成物未再导出 | sicnu_geo_rs_cli 构建（qgis_gui 依赖，窗口式续跑） | build9 | 进行中（机器上另有 3 个并行 track 构建，资源争抢） | PART | export-catalog + drift gates
- R9 | review P0/P1 未修 | 14 项 finding 全部处置（2 P0 / 3 P1 / 4 P2 / 5 P3）+ 2 个自发现（swap_ranges UB、scale minSamples）+ BenchmarkService 限定（#992 pre-existing） | 8 套件 | 全绿 | PASS | 双验证
- R10 | Oracle 6 双验证 | 8 套件原样连续两遍 | sic11-test.cmd ×2 | PASS1 8/8 exit0、PASS2 8/8 exit0（drift 4566 断言×2） | PASS | rebase+PR
- R11 | Oracle 7 PR | push + gh pr create（不 merge，不等在线 CI） | git push; gh pr create | PR #1021 已创建 | PASS | 终态达成
# .goal-loop-ledger — teaching-lab-platform-11

Round 1 | Oracle gap: none yet (Phase 0 setup) | focused change: worktree + planning artifacts | verify: git status clean after commit | result: (this commit) | PASS | next: Phase 1 pack contract
Round 2 | Oracle gap: no pack contract (A) | lab_data_pack module + 17 packs + drift tests | pending compile | WIP | Phase 1 code complete
Round 3 | Oracle gap: labspec labs 8-11 ungraded (B) | 6 new kernels + output_verifier integration + 4 rules files + fixture gen + corpus | pending compile | WIP | Phase 2 code complete
Round 4 | Oracle gap: batch identity/summary (C), report CLI + recorded grade (D) | batch v2 + lab_report_runner + tests | pending compile | WIP | Phase 3 code complete
Round 5 | Oracle gap: injection corpus (E), self-check (F) | lab_injection_corpus + evals + lab_self_check + tests | pending compile | WIP | Phase 4 code complete
Round 6 | Oracle gap: scale evidence (H) | test_lab_scale bounded+opt-in | pending compile | WIP | Phase 5 code complete
Round 7 | Oracle gap: corpus/matrix failures | root causes fixed: LabDataPackResult.ok flag, QSet insert, optional-of-incomplete, usageError ref/ptr, binary-mode detection (max_false_alarm_rate-only), positional band indexing (OOB segfault), zone band guards, file_check expected evidence, roster header, self-check rules probe for file-mode, validator mode counting | verification: targeted matrix 11/12 suites green; labspec 2 pre-existing | PASS
Round 8 | Oracle gap: scale evidence | 1000-submission run green (9819 assertions); docs-drift crash shown pre-existing | PASS | next: review + PR
Round 9 | Oracle gap: review P1/P2/P3 | all 13 findings remediated (R1-R13 in REVIEW_LOG.md) | full matrix + calibration reruns | PASS
Round 10 | Oracle gap: final verification | rebase clean (master unchanged @a5b11b7f); double verification runs 1+2 ALL GREEN (12 suites x 2); hygiene gates clean (diff --check, conflict markers, secrets, packs zero-diff) | PASS
Round 11 | Oracle gap: PR | branch pushed; PR #1026 created (not merged, no CI waited on) | https://github.com/WindWang2/exp-rs/pull/1026 | PASS | track terminal state reached
# GOAL LOOP LEDGER — scientific-contract-verification-11

格式：`Round N | 当前 Oracle 差距 | 单一聚焦改动 | 验证命令 | 实际结果/exit | PASS/FAIL | 下一步`

```text
Round 1 | Oracle 未启动（Phase 0 审计） | Phase 0 只读审计 + worktree + planning 落盘 | git rev-parse; gh pr/issue list | exit 0; baseline a5b11b7f | PASS | configure + 库栈构建
Round 2 | Oracle-1/2 无机制载体 | census 模块 + io:/cartography: 契约 + exemptions | commit 3d73c91e | 提交（待编译） | PASS* | metamorphic/reference/failure 测试
Round 3 | Oracle-3 无 oracle 有效性证明 | metamorphic/numeric/kill/failure 测试 | commit 73e6b380 | 提交 | PASS* | ladder + cross-surface
Round 4 | Oracle-5 ladder Windows 缺口 | ladder .exe/capability + cross-surface gate | ast+--list | exit 0 | PASS | 构建库栈
Round 5 | 全部构建被一个 master TU 阻塞 | P0: pipeline_run_coordinator <fcntl.h>（Q_OS_WIN 分支缺 include） | git diff origin/master 证实预存 | 构建 1-4 号 | PASS | data_platform_tools BenchmarkService
Round 6 | 第二个 master 编译缺陷 | P0: BenchmarkService using-declaration | 同上 | build5 | PASS | 自查 bug（rfind_last_of）
Round 7 | 自有代码编译错 | find_last_of / catch2 括号 / vexing parse / contracts using | build6-8 | build8 exit 0 全部目标构建成功 | PASS | census 首轮 gate
Round 8 | census gate 3/7（18 失败断言） | final 类名提取 off-by-one + first-party 过滤 | gate_census2 | 622/629 | FAIL→部分 | direct-stamp/宏类/链扩展
Round 9 | 5 失败断言 | 多候选切片 + direct-stamp 扫描 + 宏体事实 + 祖先链扩展 | gate_census3/4 | MSVC ICE → 修 | FAIL | 17 条缺失契约
Round 10 | coverage gate 抓到 17 个 live rs: 无契约 | 一次性补齐 17 条记录（model-task/InSAR 链/temporal-10/CN import/spectral tools） | gate_census5 | All tests passed (768 assertions in 7 cases) | PASS | graph 快照 + 其余 gates
Round 11 | 快照再生 + 其余套件 | census/graph 快照再生；PROJ_DATA 环境；重链接旧 gate | 9 套 gate | 4 绿 5 报 42 | PARTIAL | 逐套修复
Round 12 | M4 抓到 band_ratio NoData 泄漏（真缺陷） | bandRatioTile 掩膜重载 + ratio 路径接入 sentinel | M4 hole pixel (4,4) value 1 → 修复后 NaN | metamorphic 1122 ✓ | PASS | failure F5/claims/cross-surface
Round 13 | failure F5 宿主不可靠 / claims 对 io: 误判 | 不存在目录=确定性拒绝；只读降 WARN；绑定 census schemaGrade | gate | failure 32 ✓ determinism 138 ✓ numeric 234 ✓ | PASS | cross-surface 工具面
Round 14 | tools.json 绑定面混类 | 幻影检查=实现存在性（src/agent 全源 literal）；graph gate 按 scanner-scope 精确清单 | gate | cross-surface 361 ✓ | PASS | known_answer 105 ✓；drift/platform_9 分类为 master 预存
```

当前 Oracle 状态：
1. live operator 契约或豁免 → census gate 768 ✓
2. determinism 有执行证据 → replay corpus + triple-publication 138 ✓
3. metamorphic/reference 抓变异 → mutation_kill 42 ✓ + M4 真缺陷实证
4. targeted lanes 连续两次 → 待 Phase 8 双跑
5. diff 干净 → 待 Phase 8 检查
6. 关键验证双跑 → 待 Phase 8
7. 独立 review → 待 Phase 7

最终状态（2026-09-16，HEAD = 分支 tip，已推送，PR #1027 已创建未 merge）：

```text
Round 15 | Oracle-7 独立 review 收尾 | subagent#2 对抗 review 18 findings（P0=0/P1=3/P2=5/P3=10）→ 修复 F-01..F-09/13/14/16/17/18，F-10/11/12/15 disposition=deferred | gate 双跑 + ladder 双跑 | P1=0；10 套件两遍逐套一致；ladder 两遍判定一致 | PASS | rebase(a5b11b7f 无新提交)+push(重试 2 次后成功)+PR #1027
```

Oracle 终判：
1. live registry 每算子契约或豁免 → census_11 770 ✓（17 个漏记录被 gate 抓获并补齐）
2. determinism 有执行证据 → determinism_11 138 ✓（replay corpus + triple-publication）
3. metamorphic/reference 抓变异 → mutation_kill_11 42 ✓（10 变异全抓）+ M4 实抓 band_ratio NoData 真缺陷并修复
4. targeted lanes 连续两遍 → Run1/Run2 逐套一致全绿 ✓
5. diff --check/冲突/secret → 干净（GOAL.md 逐字存档尾部空格为刻意例外，EVIDENCE 已记录）✓
6. 关键验证双跑 → 10 套件 + ladder 两遍 ✓
7. 独立 review P0=0 P1=0、finding 全部 disposition、PR 已创建未 merge → PR #1027 ✓
# GOAL LOOP LEDGER — linked-visual-analytics-11
Round 0 | baseline audited (a5b11b7f), worktree created, planning seeded | n/a | n/a | n/a | PASS | Phase 1: VaSelectionHub
Round 1 | VaSelectionHub absent (WP-A gap) | typed hub: generation, echo suppression, bounded history | build test_visual_analytics | 10/10 cases, 100048 asserts PASS | PASS | Phase 2 controllers
Round 2 | ViewLinkController unmounted, no groups/history/cursor | groups + history/undo + cursor link + commands + shell wiring | build (in progress) | pending | WIP | run tests
Round 3 | review verdict P0=1 P1=3 | P0-1 displayed-payload pick, P1-1 context restore, P1-2 rotated-gt fail-closed, P1-3 removeView disconnects, P2-2 Result check, P2-3 per-instance origin | code review + regression tests | committed 02bca69a | PASS | finish build, run gates
Round 4 | QGIS-canvas test binaries hung at init on host | cdb stack dump → missing qca-qt6.dll/qt6keychain.dll hard-error dialog; runner PATH fixed | full gates | view_link 84 asserts PASS, visual_analytics 100056 PASS, dvs PASS, contract 204 PASS | PASS | pass 2
Round 5 | Oracle 6 double validation | all four gates re-run | second full pass | all EXIT 0 / All tests passed | PASS | finalize PR
Round 6 | terminal state | push + PR created (not merged, no online-CI dependency) | gh pr create | https://github.com/WindWang2/exp-rs/pull/1028 | PASS | track complete

Worktree `../exp-rs-worktrees/ds41-remote-pool-init`, branch `agent/ds41-remote-pool-init`,
appended below is this track's goal-loop ledger (round log, ownership list, oracle
evidence, review-gate record and post-review double validation).
Findings from the two independent reviewers (implementation + concurrency expert and
tests/build reviewer) were remediated in the two commits above; see
"Review gate" below for the line-by-line dispositions.
# GOAL Loop Ledger — ds41-remote-pool-init (issue #1047)

Track: 17 — fix(data): make RemoteDatasetPool first-use initialization thread safe
Worktree: `../exp-rs-worktrees/ds41-remote-pool-init` · Branch `agent/ds41-remote-pool-init`
Baseline: `origin/master = 2caac836c4bfb7f62a317d19cf5257c8a97f8388` (re-fetched before worktree add; master read-only from main checkout)

## Ownership (this track only)
| Path | Ownership |
| --- | --- |
| `src/data/providers/remote_source_cache.h` | PRIMARY (Track 05 reserves it for this task) |
| `src/data/providers/remote_source_cache.cpp` | PRIMARY |
| `tests/test_remote_pool_init.cpp` (new) | PRIMARY (precise concurrency regression) |
| `tests/CMakeLists.txt` (only the new lean target block) | minimal shared touch |
| `.goal-loop-ledger.md` | append-only (this section) |

Reserved elsewhere (NOT touched): WorkspaceCatalog/ArtifactStore/GovernanceStore (issue #1045 track), `src/geospatial/**` non-pool files, app/UI.

## Discovery (read before any write)
- Open PRs at start: none (`gh pr list --state open` empty) → no dedup/skip needed.
- Merged context read: #1030, #1031, #1032 (latest), #1029–#1008.
- Open issues read: #1033–#1056 incl. target #1047 (P1, remote pool init race) and review umbrella #1037.
- Worktrees/branches: only `fix/review-issues-1033-1056` sits at the same base commit (sibling review lane, scope #1033/#1034/#1035/#1036/#1037 — NOT #1047). No branch or worktree contains changes to `src/data/providers/remote_source_cache.*` (`git log --all -- <paths>` → only the original Phase-F commit).

## Static proof of #1047 (confirmed by reading, not assumed)
`acquire()` tested `m_impl` and allocated `new Impl` with no synchronization; `clear()` read it unsynchronized. Two concurrent first acquires could each observe nullptr → two `Impl`s (one leaked), two mutex domains, two `handles` maps, two independent `handlesPerUrl` bounds; the `handlesPerUrl` clamp write raced with the read in the hot loop.

## Chosen fix
- Removed the raw `Impl *m_impl` member; added private `static Impl &impl()` returning a function-local static (C++11 magic-static guarantee) shared by `acquire()` and `clear()`.
- Env `SICNU_REMOTE_POOL_HANDLES` parsing/clamp moved into `Impl` construction with `const size_t handlesPerUrl`, so every reader sees a fully-published value.
- `clear()` additionally waits for in-flight opens (`opening` map drain, zero-count entries tolerated) so "drops every cached handle" is honest under concurrency.
- No public API/ABI change: `instance()/acquire()/clear()/openCount()` signatures unchanged; the removed member was private.

## Oracle checklist
- [x] O1: N threads concurrent first acquire → exactly one Impl / no leak / one mutex domain (tested: 1 distinct `GDALDatasetH`, exactly 1 `GDALOpenEx` at bound=1, all threads served).
- [x] O2: `handlesPerUrl` bound cannot be bypassed (peak concurrent holders ≤ bound, open growth ≤ bound).
- [x] O3: acquire/clear contract explicit + tested; two consecutive full-suite passes (plus a third) green.

## Round log
Format: `Round | Oracle gap | single focused change | verification command | result/exit | verdict | next`

| Round | Oracle gap | Focused change | Verification | Result | Verdict | Next |
|---|---|---|---|---|---|---|
| R0 | none (facts) | Discovery: origin/master refresh, PR/issue/branch audit, worktree add | `git fetch`, `gh pr/issue list`, `git worktree list` | base `2caac836c` = prompt reference; 0 open PRs | PASS | implement fix |
| R1 | all | Thread-safe Impl init (magic static + Impl ctor env clamp) + clear() in-flight drain | `cmake --build build-dev --target test_remote_pool_init -j1` | exit 0 (pre-existing `getenv` C4996 warning unchanged) | PASS | add regression + run |
| R2 | tests missing | New cross-platform concurrency suite `tests/test_remote_pool_init.cpp` + lean CMake target | `ctest -R RemoteDatasetPool -j1` | **4/4 TIMEOUT** | FAIL | diagnose (own new code under suspicion) |
| R3 | clear() spins forever | Found: `opening.empty()` is never true because `operator[]` leaves zero-count entries → clear() deadlock | diagnosis via crumb traces | all 4 now run | PASS | fix predicate |
| R4 | 2 tests still timeout | Fixed predicate to `all_of(... second==0)`; also found **my test's own bug**: final `const auto again/lease` still in scope when calling `clear()` (contract blocks for leases) | scoped the lease; removed debug instrumentation | `ctest` 4/4 Passed | PASS | double-run Oracle |
| R5 | Oracle evidence | Green pass #1 and #2 consecutive | `ctest -R "RemoteDatasetPool" -j1` ×2 | 4/4 passed both runs (2.71 s / 2.96 s) | PASS | red demonstration |
| R6 | regression must be real | Reverted pool .h/.cpp to master (build red) and ran the same test ×10 | direct exe runs | **8/10 FAILED pre-fix**: `distinctHandles 4==1`, `peak 3==1`, `opens 4==1` — exactly issue #1047 | PASS | restore + final verify |
| R7 | final state | Restored fix, rebuilt, green pass #3 | `ctest -R "RemoteDatasetPool" -j1` | 4/4 Passed | PASS | review + commit + PR |

## Evidence notes
- Local lane: Windows MSVC (the repo's Windows lane), focused target only, `-j1` (`CMAKE_BUILD_PARALLEL_LEVEL=1`, `CTEST_PARALLEL_LEVEL=1`), no `-j3+`, no other builds alongside.
- `test_offline_mode` is `NOT_BUILT` in this fresh worktree tree: it needs the full `sicnu_add_test` link set which this targeted build never pulled (10 384 tests NOT_BUILT overall — the normal state of a freshly configured, partially built tree). Not a regression from this change.
- The POSIX-only `test_remote_source_cache` (loopback HTTP server) is unchanged and is the CI lane's home for the remote-transport side of these assertions.


## Review gate (independent, two reviewers) + remediation

Reviewer A (implementation) findings and dispositions:
- MAJOR exception hole in acquire() unlock/lock window: a leaking opening reservation would drop the per-URL bound and, with the new clear() drain, wedge clear() forever. FIXED: handle allocation and url.toUtf8() now run while the pool mutex is held; a try/catch(...) around GDALOpenEx restores the lock + reservation and rethrows; push_back failure now GDALCloses the open dataset before rethrowing.
- MINOR env-latch contract undocumented (first clear() can now latch the bound). FIXED: instance() doc states the bound is read once and latched.
- Remaining accepted risks (recorded, not silently ignored): clear() remains unbounded in wall clock if a lease is never returned or an open is stuck at GDAL timeout bounds; zero-count opening entries persist per URL (bounded by distinct-URL count).

Reviewer B (tests/build) findings and dispositions:
- MINOR case 1 was not a concurrent first use: pre-storm pool.clear() built the Impl on the main thread, so the fixed build storm only saw an already built pool. FIXED: clear() removed from case 1 (documented in-file); the case now performs the genuine first use, so it regresses the bug under the fixed design too.
- MINOR driver->Create() unchecked. FIXED: REQUIRE( ds != nullptr ).
- MINOR CMake comment suggested a ctest selector that matches nothing (cases are discovered under Catch2 names). FIXED comment.
- NIT unused stop flag. FIXED: removed.

## Post-review verification (final Oracle evidence)
- Build after remediation: cmake --build build-dev --target test_remote_pool_init --parallel 1 -> exit 0 (only the pre-existing getenv C4996 warning, unchanged from master).
- Green: ctest -R "RemoteDatasetPool" -j1 twice consecutively -> 4/4 passed both runs (2.82 s / 2.94 s).
- Potency (red), pre-fix implementation with the final test file:
  - 16 threads: 10 runs -> 8 failed (distinctHandles 4==1, peak 3==1, opens 4==1) — exactly the two-Impl / bound-bypass symptom of #1047.
  - 32 threads (final): 3 runs -> 1 passed, 2 hung > 45 s (killed) — the pre-fix race also livelocks the pool.
- Green at 32 threads: 10/10 direct runs, avg ~1.1 s each.

# GOAL LOOP LEDGER — ds41-fuzz-boundaries (parser/IPC/path/checkpoint boundary fuzz track)

Baseline: origin/master adf8f9895 (fresh at Phase 0). Worktree: exp-rs-worktrees/ds41-fuzz-boundaries, branch agent/ds41-fuzz-boundaries.

| Round | Oracle 差距 | 聚焦改动 | 验证命令 | 结果/exit | 判定 | 下一步 |
|---|---|---|---|---|---|---|
| 0 | 全部（Phase 0） | fetch/PR/issue/branch census + 源码级 entry census + worktree | git/gh 只读命令组 | open PR=0, open issue=0；9 个远端分支全部 superseded | PASS | planning 工件 |
| 1 | Oracle 输入清单 | BASELINE/OWNERSHIP/DEDUP/ORACLES/DECISIONS.md | 文件落盘 | 5 文档 + .gitignore 白名单 | PASS | worktree + configure |
| 2 | 工具链 | worktree add + configure build-dev | cmake --preset dev-default | configure 312s+119s, exit 0 | PASS | 建 lane |
| 3 | O1/O2 lane 缺失 | tests/support/fuzz_corpus.h + frame/payload/paths lane + CMake append | ninja -j1 test_contract_fuzz_* | 编译通过（修 Ipc/IpcFrame 命名、filesystem include、u8 路径构造） | PASS | 跑 lane |
| 4 | O1 零 crash | 首跑 lane | test_contract_fuzz_frame/payload/paths | frame: **SIGSEGV stack overflow**（depth 866）；payload: 4 类 Json::LogicError；paths: ANSI 码页 system_error | FAIL→发现 5 个缺陷 | 根因定位 |
| 5 | O4 corpus/最小化 | depthprobe 二分 + 5 个 production 修复（stackLimit+guard、commandId、required、diagnostics、UTF-8 path） | 重跑 lane + 定向 probe | D1-D5 全部转为 typed reject | PASS | 测试期望修正 |
| 6 | O1/O2 稳定 | 修正测试自身缺陷（jsoncpp 抛异常、初始化和类型推导、NUL 字面量、嵌套 JSON 拷贝扁平化、贡献预算、事件词汇、DDmin 1-minimality 语义） | 重跑 lane | frame/payload/paths 全绿 | PASS | 双跑 + 回归 |
| 7 | O6/O7 双跑 | 两次连续跑 + test_exprs_ipc/test_plugin_ui_schema | 目录内直跑 | 两遍全 PASS；`split config fuzz` 为**预先存在**环境失败（master 代码同样失败，Qt 内部 assert） | PASS（记录限制） | sanitizer lane |
| 8 | O5 sanitizer | cmake --preset dev-default -B build-san -DENABLE_SANITIZERS=ON；ninja -j1 三 lane；ASan 运行 | configure 181s+71s；165 steps | 零 AddressSanitizer 报告（需 ASAN_WIN_CONTINUE_ON_INTERCEPTION_FAILURE=1） | PASS | 独立 review |
| 9 | O8 review | 独立 read-only reviewer 审 origin/master...HEAD | agent review | SHIP-WITH-FIXES：2×P1（parseFrame 无 guard、stackLimit 可能拒绝合法 event）+ 10×P2 + 4×NIT | FAIL→修复 | 逐条处置 |
| 10 | O8 清零 | F1-F14 全部处置（parseFrame guard、maxEventValueDepth=32 + 传输上限 128、删死代码/加真实 size leg、修不可达分支、invalid-UTF8 语料、DEDUP 更正、文档、allowComments 保持） | 重跑 lane + 回归 + ASan | frame 13111/12、payload 6475/10、paths 6692/4 全绿；ASan 零报告 | PASS | 二次 review 确认 |
| 11 | O9 PR | PR body + push + gh pr create | gh pr create | 见 PR URL | PASS | track 收口（不 merge、不等 CI） |

## Notes / 环境观察（已记录到 tests/corpus/README.md）
- 本机沙箱 `cmd.exe /c` 不可用 → 构建经 PowerShell 导入 vcvars 环境。
- 并发 session 曾删除所有本地 `agent/*` 分支 ref（7 个 worktree 同时中招）；以 commit SHA 重建并提前 commit 保护对象。
- vcpkg 全局锁导致 -j1 构建排队；重 target 曾误拉 qgis_core（~3000 units），改为 SDK-light target 集。

## ds41-fuzz-boundaries — PR created (not merged, CI not awaited)

PR: https://github.com/WindWang2/exp-rs/pull/1123
branch agent/ds41-fuzz-boundaries, base master, 25 files, +3564 / -66.
Oracle final state:
- O1 lanes green (frame 13111/12, payload 6478/10, paths 6692/4) — two consecutive passes.
- O2 round-trip + typed-reject properties stable.
- O3 mutator hits 5 proven defect patterns (D1-D5) + 2 follow-ups found in review.
- O4 every defect has a minimized fixture + root cause in tests/corpus/README.md.
- O5 sanitizer lane (repository ENABLE_SANITIZERS=ON, build-san, -j1): zero AddressSanitizer reports.
- O6 existing suites (test_exprs_ipc, test_plugin_ui_schema) pass unchanged; the one failure
  (test_contract_fuzz_ipc "split config fuzz") is a pre-existing environment issue reproduced
  identically on master code.
- O7 two consecutive passes recorded.
- O8 independent review: first pass SHIP-WITH-FIXES, second pass SHIP with no residual P0/P1;
  all findings dispositioned (F1-F14 + re-review follow-ups).
- O9 PR created (this entry). No merge; no CI wait (per track rules).

## Post-PR continuation (follow-up round)

- Drift re-check after the PR: `git fetch origin --prune` — origin/master still at the
  baseline adf8f9895; PR #1123 OPEN/MERGEABLE.
- Parallel-track overlap audit (PRs #1116-#1122, all opened after this track started):
  none touches the src/ files this branch modifies; all seven append to the tail of
  tests/CMakeLists.txt (same append-only convention), so the only expected conflict is
  textual at the end of that file — recorded in the PR body.
- PR body refreshed with the final assertion counts (payload 6478 after the wide-value
  case) and the conflict-hotspot audit; `gh pr edit 1123 --body-file`.
- Third consecutive verification pass after the PR: frame 13111/12, payload 6478/10,
  paths 6692/4, test_exprs_ipc 112/23, test_plugin_ui_schema 25/8 — all green.
- Remaining known limitation (unchanged, documented in the PR): the heavy suites that
  reach the SDK through the full qgis closure (test_plugin_manifest,
  test_plugin_ui_schema_host, test_exprs_workflow_schema) were not rebuilt locally —
  their contracts are covered by the light lanes and by the two unchanged suites above,
  and a from-scratch qgis_core build at -j1 is out of proportion to the added evidence.
