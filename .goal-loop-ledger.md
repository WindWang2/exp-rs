# GOAL-LOOP LEDGER — closure-workflow-contracts-r4 (Track 2)

Track: Workflow / Agent / Scientific Semantics / Provenance / Contract 一致性收口 (R4 integration closure)
Branch: hardening/closure-workflow-contracts-r4 · Base: 15e5c66b543ef3874cb929f17529ef456bd6c059 (origin/master @ 2026-09-26)
Build: Debug, -j2 hard cap (CMAKE_BUILD_PARALLEL_LEVEL=2), OTB/Python/OpenVINO/TensorRT OFF, GDAL/PROJ from /home/kevin/pwb-sdks/root/usr.

## 总 Oracle（Track 2）

1. baseline 可复现的 workflow/scientific/provenance/mission/contract 失败全部根因分类；
2. 产品 bug 已修；stale contract/test 有 canonical evidence；
3. 核心 tests（workflow facts/repair/analysis、provenance projection、mission persistence/parity、preflight/grounding、execution authority）通过；
4. canonical vocabulary 无双 source-of-truth；
5. resume/retry/cancel/repair 不绕过 authority、不重复 side effect；
6. provenance/verifier/explain evidence 与真实执行一致；
7. contract/snapshot 更新有明确源头；
8. touched hot path 有复杂度/缓存一致性检查；
9. 关键 Oracle 最终 commit 上连续通过两遍；
10. `git diff --check` 通过；
11. 独立 reviewer 无未解决 P0/P1；
12. rebase 最新 origin/master 后重新验证通过；
13. 已 push 独立 branch 并创建 PR。

## 启动快照（动态基线扫描，2026-09-26）

- master = 15e5c66b5（动态扫描确认，未漂移）；#1333 最新合并。
- Open PR: #1334 fix/review-p1-security（MERGEABLE；安全收口：sandbox/PluginHost/model runtime/keychain）；#1335 fix/review-p0-build-restore（MERGEABLE；ci.yml/CMake/ctest wiring，18 files）。
  - 二者为高冲突区：不复制其修复；本 Track 优先不重叠文件；若必须依赖仅建临时诊断 worktree；正式分支始终 origin/master。
  - #1335 已知改动文件（高冲突区）: .github/workflows/ci.yml, CMakeLists.txt, cmake/SicnuCatchAddTests.cmake, cmake/SicnuSharedLinkGuard.cmake, src/agent/CMakeLists.txt, src/agent_loop/CMakeLists.txt, src/app/CMakeLists.txt, tests/CMakeLists.txt, tests/support/d15_e2e_pipeline.cpp, tests/test_capability_knowledge.cpp, tests/test_data_platform_surface.cpp, tests/test_gpu_plane.cpp, tests/test_lab_data_pack.cpp, tests/test_lab_grading.cpp, tests/test_teacher_credential.cpp, tests/test_verification_env_12.cpp, tests/test_view_link.cpp, mission-runtime-gate/tests/test_mission_runtime_scale.cpp
- Open issues: 0。
- 历史分支仅考古（禁止整支 merge/cherry-pick）: agent/flash-{data-transaction-integrity, processing-atomic-errors, workflow-integrity}（behind ~794）、fix/ci-durability-atomic-fs（behind ~150）。
- #1335 本地诊断的 ~142 个历史失败分布（启动线索，须重新复现归类）: VectorWriter/GDAL atomic staging ~28; Qt/QGIS 退出崩溃 ~14; i18n 漂移 ~14; contract/snapshot 漂移 ~17; scientific/workflow/logic ~45; loopback S3/HTTP ~7; plugin ~6; timeout/scale/env ~6。

## 本 Track 范围（Track 2 = workflow/agent/scientific/provenance/mission/contract 语义收口）

不属于本 Track: VectorWriter/GDAL atomic staging、Qt/QGIS 退出崩溃、i18n、loopback S3/HTTP、Qt plugin process/lifecycle（Track 3/其他 Track）。

## 轮次记录

### Round 0 — 基线扫描与 worktree 建立（2026-09-26）
- worktree: /home/kevin/project/exp-rs-closure-workflow-contracts-r4，branch hardening/closure-workflow-contracts-r4 @ 15e5c66b5。
- 旧 ledger（flash-spectral-intelligence-13 / PR #1145 遗留）已被本 ledger 替换；旧内容在 git 历史中。
- 下一步: 配置 build-track2 → 枚举并编译 Track2 测试目标（-j2，后台）→ 运行 baseline → 失败簇映射（A: workflow facts/repair/analysis/checkpoint; B: scientific facts/units/grounding/preflight; C: provenance/verifier/evidence; D: mission runtime/persistence/authority parity; E: contract/capability/surface/snapshot; F: plugin∩agent contract 交界）。

### Round 1 — 考古报告（Subagent 1）与修复假设清单（2026-09-26）

考古结论（详见对话报告，关键项摘录）：
- flash-workflow-integrity / fix/ci-durability-atomic-fs：思想已全部被 master 更强实现吸收（checkpoint 上限 16MiB、quarantine、threading contract、containment、atomic_fs 链接）→ 无残缺价值。
- flash-data-transaction-integrity：checked 事务已落；**残留缺口** = governance_store 无 `SICNU_FAULT_POINT` 注入点 + 无 commit-failure fault-matrix 测试（备选收口项）。
- flash-processing-atomic-errors：#1247 已吸收大半；**真实残留** = `src/processing/providers/qgis_algorithms/native_union.h:136,187` 的 `if (feedback->isCanceled()) break;` ——取消后正常返回，部分 vector 输出报成功（对应簇 C "partial/cancelled run 错误标 success"）。

修复假设清单（按簇，待 baseline 失败确认后逐轮执行）：
- **B1（vocabulary）**：`"dn"` vs `"digital_number"`。`f578db20c`（#1230）故意让 SAR 产品 stamp `"dn"`（理由：rs:sar_calibrate 词汇门拒绝光学 digital_number；golden fixture gf3_sar_valid.json 已同步），但漏改 3 处测试断言（test_cn_product_families11.cpp:152、test_product_import_plan11.cpp:407/414）→ **stale test，待 baseline 复现后同步**（GF-1 光学两处断言正确，不动）。canon 层：护照 closed set 在 scientific_state；四张并行别名表（scientific_state normalizeRadiometricToken / science_context capability_router::canonicalRadioToken / harness workflow_analysis::deriveNumericDomain / core radiometric_state UPPERCASE）+ 2 处裸子串匹配缺陷（capability_graph.cpp:296-302、scientific_preflight.cpp:244-246 — "digital_number" 不含子串 "dn"，raw-DN 建议对光学 canonical 值永不触发）。
- **B2（fail-open）**：core/radiometric_state.cpp 未识别值静默降级 DigitalNumber（违反 fail-closed）→ 待查调用方与测试后定修法。
- **C1（cancel=success）**：qgis_algorithms native_union 等 vector sink 取消 break → 需按 #1247 的 error contract 收口（checkCanceled throw）。
- **A/R2（approval 绑定语义）**：approval 绑 findings_digest 而非 planId —— #1327 设计意图（"deterministic re-projection = 同一绑定目标"），需以测试 pin 语义而非改行为；provider 变更后同 findings 换 plan 的场景补 oracle。
- **C/R4（provenance 字面量 4 处散布）**：d17_provenance kind/version 在 workflow_provenance.cpp（writer/reader）、verify_adapters provenance_sidecar_view.h:33、grader_adapters.cpp:160 各持字面量 → 收敛到单一常量头。
- **B/R3（typed error 塌缩）**：data_platform_tools.cpp:1638-1656 把 PassportResolution 的 gdal_open_failed/asset_not_found/resolver_unavailable 塌缩为 Unknown → 投影 typed reason。
- **C/R7（size cap 不一致）**：verify_adapters bounded_io vs explain file_too_large 对同一 sidecar 阈值不同 → 统一。
- **E/R6（seed 字段名漂移）**：reproduction_bundle `seed_hex` vs study_export `seed_u64` → 导入互认 + 文档 canonical。
- **A/R5（timeline cursor）**：missionTimelineCursorRetained 调用点审计。

### Round 2 — workflow_facts_11 六失败全修复（2026-09-26）✅

Baseline: 25 cases / 6 failed → 修复后 25/25 pass（188 assertions）。

根因分类（全部为产品实现 bug，其中 1 个为测试数据编码 bug）：
1. `medianOfSorted` 被喂了未排序 gaps（[1d,59d,1d] 的中位数读成 59d）→ gapDeviations 与 regularity 全错。修：排序副本再取中位。
2. dedup 仅按 epoch 合并精度，"同日不同精度"（10:00Z vs date-only）不合并 → 混合精度列表 count 虚高、首尾形态漂移。修：按 UTC 日桶去重，coarsest precision 胜出（date-only 接管代表位，epoch 收拢到当日午夜）；同日两个不同时刻的 timestamped 观测仍是两个观测。
3. cadence label 无 regularity 门控 → irregular 序列的 1d 中位 gap 被标成 "1d" cadence（把噪声包装成重复周期）。修：仅 kRegular/kNearRegular 计算 monthly/annual/<N>d。
4. `levelSuffix` 未小写化（头文件规格 workflow_facts.h:202 明确 "lowercased"）→ "L2A" 产出 "A" ≠ "a"，digest 也随之不稳定。修：toLower。
5. `workflowFactsDigest` 返回 32 hex（left(16) bytes→hex）vs 仓库 sha256/16 约定（repair_planner findings_digest 同宽）16 hex。修：left(8) bytes→hex。
6. 测试数据 bug：GEOGCS WKT 用例把 C++ 相邻字符串拼接语法写进 raw string（`"PRIMEM..."` 的引号成为 JSON 文本）→ Json::Reader 拒绝。修：合并为单行 JSON 字符串（raw string 内不存在拼接）。

注：:211 原 `count == 4` 与同文件 :163 去重测试的契约矛盾（4 = 原始条目数，未做同日合并）；按去重契约（:180 显式期望 + impl 注释"coarsest wins on collision"）修正为 3 并补注释。未放宽任何断言（regularity/label/first 断言全保留，另加 first 断言）。

验证: `./tests/test_workflow_facts_11` → All tests passed (188 assertions in 25 test cases)。
Commit: fix(harness): cadence day-dedup, sorted-median, label gating, mechanical level suffix, 16-hex facts digest
