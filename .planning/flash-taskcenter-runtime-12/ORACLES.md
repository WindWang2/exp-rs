# ORACLES — flash-taskcenter-runtime-12

Objective completion criteria. Each oracle names its reproducible evidence. All tests run with `QT_QPA_PLATFORM=offscreen`, `ctest -j1` inside `build-dev` (or direct exe).

| # | Oracle | Evidence (test / command) | Status |
|---|---|---|---|
| O1 | Priority is observable AND low priority never starves: under a sustained High stream with 1 worker slot, a Low task completes within a bounded wait (aging). | `test_task_center_12.exe "[tc12][fairness]"` — `setAgingIntervalMs(50)`; asserts aged Low outranks at least one fresh High while the first High still precedes it (priority observable). | green ×2 (72 asserts); re-run pending after review-fix pass |
| O2 | queue/RAM/in-flight hard bounds + stress: non-terminal task count ≤ `maxPendingTasks` (submission refused, not dropped, when full); in-flight ≤ globalMax; RAM budget holds. | `test_task_center_12.exe "[tc12][backpressure]"` — setMaxPendingTasks(2), submissions 3-4 refuse with −1 + `tasks_refused` counter; `submitPipelineJson` refuses atomically (no partial records). | green ×2; re-run pending |
| O3 | Cancel reaches a unique terminal outcome in every state: queued, admission-held, running, retry-pending, cascade child, shutdown; no duplicate completion callbacks. | `test_task_center_12.exe "[tc12][cancel]"` + `[tc12][fault]` — watchdog finalizes a hung-worker cancel (150 ms deadline, typed reason preserved, `watchdog` in errorMessage); swallowed job-record fault + cancel → watchdog is the only finalizer; late records are foreign no-ops. | green ×2; re-run pending |
| O4 | Provider crash/retry: old job record never pollutes the retry job; transient error → in-place retry with fresh jobId; exhausted budget → Failed once. | Covered by `test_task_center` (382 asserts ×2 green) and `test_execution_plane_8` (36k asserts ×2 green) retry/routing cases — unchanged semantics; resurrection now clears cancel bookkeeping (review P1 fix). | green ×2; re-run pending |
| O5 | Core TaskCenter/execution tests pass twice consecutively; no data-race design gaps (all shared state under m_mutex or lock-free staging; no engine calls under m_mutex). | `test_task_center_12` ×2, `test_task_center` ×2, `test_execution_plane_8` ×2, `test_execution_plane_9` ×1 (16 pass, 1 env-gated skip). `test_execution_plane_7` worker-route case hits a PRE-EXISTING Qt thread-affinity assert — reproduced on unmodified master; not a Track 12 regression. | green ×2 on the pre-review build; re-run pending after review fixes |

Secondary (WP completeness, not gating):
- O6 observability: `TasksSubmitted/TasksFailed/TasksCanceled`/`TasksRefused`/`TasksAged`/`CancelWatchdogFired` counters on the real paths; QueueWait/Dispatched/ExecutionStart/ExecutionEnd/Cancelled events + `RssSample` once per candidate-examining admission pass. → `test_task_center_12.exe "[tc12][telemetry]"` (green ×2; RssSample assertion added in review-fix pass, re-run pending).
- O7 admission weight dims: `execution.cpuThreads` + `diskRead/Write`/`networkWeight` keys honored; `admissionSnapshot` mirrors every gate of the real pass INCLUDING the idle never-starve rule (m_active.total == 0). → `test_task_center_12.exe "[tc12][weights]"`, `"[tc12][cpu]"`, `"[tc12][snapshot]"` (cpu + idle-fidelity cases added in review-fix pass, re-run pending).
- O8 estimator never runs under `m_mutex` (enqueued tasks carry a warm estimate). → shared lock-free warm helpers (`descriptorDimsForAlgorithm`/`estimateMbForAlgorithm`) feed enqueueTask/submitPipeline/admissionSnapshot; warm-cache consistency asserted via snapshot cases.
- O9 latency lanes: explicit `ExecutionRequest.latencyClass` wins over the source→lane map; `gui`/`mcp`/`agent`-family sources admit against the full weight cap while batch stays reserve-bounded. → `test_task_center_12.exe "[tc12][latency]"` (added in review-fix pass, re-run pending).

Gate rule: O1–O5 must pass twice consecutively before PR; independent review P0/P1 = 0.
