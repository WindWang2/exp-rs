# GOAL LOOP LEDGER — ds41-workflow-durability-13

Format: Round N | Oracle gap | single focused change | verification command | result/exit | verdict | next step

Round 0 | All (Phase 0 pre-read + dedup) | live fetch + PR/issue/branch census + code read of both workflow stacks + planning docs + worktree from fresh origin/master | git fetch / gh pr list / gh issue list / new_worktree.py | origin/master=79adfe78a; open PRs #1135-#1137 (+#1138 later) none in workflow domain; 4 known limitations confirmed by reading master code (sha256fl blind spot, _resume election collision, provenance filename collision, mutator affinity holes) + 1 adjacent hole found (generated-id resume ghost never grouped) | PASS | Round 1: WP1 identity + WP3 affinity
Round 1 | O1/O3 no carrier | pipeline_run_coordinator: sha256full (chunked, cancel-polled) + ArtifactIdentityMode{Auto,Full,Full} default Auto; tag-driven verification; two-phase requestCancel; all mutators marshalled via one pattern; destructor flag+drain; provenance attempt-N/ layout; publish fault points; fault_registry compiled into light targets | cmake --build --target test_workflow_checkpoint_cache; run binary | 29/29 cases green (691 assertions) after updating the attempt-suffix layout assertion | PASS | WP2 envelope
Round 2 | O2 no carrier | workflow_run: attempt/resumeOf envelope, version 2 with legacy-1 acceptance + named refusals; workflow_checkpoint: election by declared lineage (resumeOf), complete-lineage preference, recovery-candidate-only grouping, legacy suffix rule for legacy/corrupt; workflow_run_coordinator: resumeOf stamping before first persist + attempt increment after swap; single-TU syntax check of the heavy coordinator file | cmake --build --target test_workflow_durability_13; c++ -fsyntax-only with real flags | 9/9 cases green (188 assertions); heavy TU compiles clean (SYNTAX_EXIT=0) | PASS | D17 WP1/WP3/WP4 tests
Round 3 | O1/O3/O4 oracles | D17 lane: >2 MiB mid-file tamper caught under Auto/Full while the same fixture is served under Fast (discrimination proof); unknown tags fail closed; foreign mutators marshal; foreign startRun refused not raced; cancel during whole-file hash (512 MiB, self-calibrating latency assertion); foreign destruction drains; concurrent readers; crash at publish boundaries (fault points) with old-file integrity + no residue; corrupt provenance indifference; moved artifact never served; waitForThread helper for event pumping | cmake --build + run binary | 41/41 green (816 assertions) — after fixing a test-harness deadlock (main thread must pump events while a foreign blocking marshal is pending) | PASS | potency + review
Round 4 | "tests could pass vacuously" | mutation checks: (a) election legacy-suffix-only → user-named *_resume + ghost-election tests FAIL; (b) full hash with a middle blind spot → mid-file tamper test FAILS (executed 0==1); (c) mid-hash cancel classified Failed → cancel test FAILS (4==5 enum) | rebuild + run after each mutation, revert via git checkout | all three mutations caught; tree reverted clean | PASS | independent review
Round 5 | independent review (separate agent, reads origin/master...HEAD itself) | review of diff + planning docs + ADR | reviewer report | 0 P0 / 1 P1 / 0 P2 / 6 P3, SHIP-WITH-FIXES. P1: an idle requestCancel leaves the cancel flag set, which then refuses the next resumeFromCheckpoint (startRun clears it, resume did not). P3s: election size/readAll TOCTOU (pre-existing pattern), foreign-destruction needs a live event loop (doc note), legacy-ghost poisoning (pre-existing, D5-intended), provenance layout external tooling (note in PR), value-path marshal failure ignored (documented), test timing caveats | FIX | Round 6: fix + re-verify
Round 6 | P1 + cheap P3s | resumeOnAffinity clears a stale cancel flag after the active-run gate (a cancel arriving DURING the resume still aborts at the loop checks); election reads cap+1 instead of size-then-readAll; header documents the event-loop requirement for foreign destruction; regression test "An idle requestCancel does not poison a later resume" | cmake --build both targets; run both binaries | 42/42 D17 (803 assertions) + 9/9 durability (188) green | PASS | second-pass review + final gates

## Environment notes
- Sandbox PATH lacks cmake: use /tmp/cmake-3.30.5-linux-x86_64/bin plus `source /home/kevin/pwb-sdks/env.sh`; configure needs -DNETCDF_LIBRARY=/tmp/libnetcdf_stub.a (same as sibling worktrees).
- scripts/dev/resource_guard.py can exit 0 while the wrapped build failed (its payload exit code was masked); always grep the build log for 'error:' and check the binary mtime.
- Sibling sessions keep load ~23; builds stay at -j2 and targeted (light D17-style lanes only — no qgis/task_center rebuild).
- The 512 MiB cancel-during-hash fixture lives on /tmp (tmpfs, 20 G free).
