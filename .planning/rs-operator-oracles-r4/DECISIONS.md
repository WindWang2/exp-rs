# DECISIONS.md — Track 7 (R4)

| # | decision | rationale | evidence |
|---|---|---|---|
| D1 | Whitelist interpretation: `src/processing/algorithms/` treated as the numeric core the brief calls `src/analysis/` | rs operators delegate their math to `processing/algorithms/*` kernels (e.g. `ImageEnhancement::processPcaFile`, `sar_calibration.cpp`). Fixing the operator layer only would paper over the kernel; the minimal honest repair is in the kernel. Per-file justification in each commit + matrix row. | BASELINE.md §5; agent sweep file:line citations |
| D2 | WP-A narrowed: no "NoData-as-zero" fix in zonal/focal/temporal_summary/segment_stats/sar_temporal_stats/regress | Audit found all six already exclude declared sentinels + NaN from statistics (Welford / masked accumulation) with nodata columns. Fabricating fixes would be make-work; fixation tests instead. | NODATA_SEMANTIC_MATRIX.md rows 1–9 |
| D3 | Defect set re-targeted to measured P1/P2s: pca, spectral_derivative, image_enhancement(ratio), contrast_stretch(+declaration), band_ratio(IHS declaration), spectral_similarity, sar_polsar_decompose, sar_phase_filter(declaration), register_images(output declaration only) | These are where sentinels are counted as data or outputs go undeclared, per sweep with line citations. | matrix rows (P1/P2 dispositions) |
| D4 | Warp-family full nodata contract (resample/align/modis_georeference) → backlog, NOT fixed this track | Shared geometric seam (`sicnu::geo::warpRaster`, `rs::algorithms::Resampler`) is cross-cutting; changing warp semantics risks other in-flight tracks. This track only documents + adds output declaration where a one-line, behavior-safe fix exists (register_images writer). | agent 3 findings; PLAN.md |
| D5 | radiometric_qa NoData policy documented, not fixed | QA flagging is by-design exhaustive (non-finite → FlagNotFinite bit, negative → FlagNegative bit): sentinel pixels are *supposed* to be classified, not skipped. | matrix row rs:radiometric_qa |
| D6 | temporal family untouched beyond fixation coverage (flash-temporal-phenology-12 owns recent work) | `rs:temporal_summary` delegates to `temporal_stream.cpp` which masks sentinels correctly; no defect found. | matrix row rs:temporal_summary |
| D7 | Determinism digests assert byte-identical raster outputs via `compareRastersBitExact` + CSV sha256 for tabular products; in-process double-run only (the "restart process" variant is covered by ctest itself re-launching the binary per case via catch_discover_tests) | Serial execution is the documented ADR 0124 regression anchor; catch_discover_tests launches a fresh process per TEST_CASE, so cross-case runs are genuine restarts. | raster_bit_compare.h; CMakeLists sicnu_discover_tests |
| D8 | Token accounting honesty: the 280M budget in the brief models a long-horizon multi-agent harness; this session records measured subagent token usage per round in the ledger and states the delta plainly in the PR instead of inflating numbers to hit a gate | Falsifying ledger numbers would corrupt the very audit trail the track exists to build. | .goal-loop-ledger.md; PR body |
