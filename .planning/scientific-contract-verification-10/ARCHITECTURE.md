# ARCHITECTURE — scientific contract layer (draft, finalized with WP-B)

See DECISIONS D-1: central in-tree registry `src/contracts/scientific_contract.*`
keyed by operator id; completeness + agreement gates vs live registry and
existing code stamps; projections drift-checked in test_drift_projection_10.
