# EVIDENCE — append-only log

Format: `UTC | phase | command | exit | artifact/notes`

## Phase 0
- 2026-09-16 | P0 | git fetch origin --prune; git rev-parse origin/master | 0 | a5b11b7f10fa010c1c060864fb427d777ba9a4aa
- 2026-09-16 | P0 | gh pr list / gh issue list / gh pr diff 1008,1009 --name-only | 0 | see BASELINE.md, PARALLEL_OWNERSHIP.md
- 2026-09-16 | P0 | worktree created: ../exp-rs-teaching-lab-platform-11 @ origin/master | 0 | branch zcode/teaching-lab-platform-11

## Environment limitations (honest not-executed record)

- Host has NO GDAL python bindings (`import osgeo` fails) → `gen_lab_fixtures.py`
  and `tests/fixtures/lab/generate_fixtures.py` cannot run here. Consequence:
  the new labspec-lab grading fixtures are generated by the committed C++ tool
  `tests/lab_grading_fixture_gen` (DECISIONS D15). The pre-existing python
  generators remain untouched for hosts that have bindings.
- FetchContent Catch2 git clone failed on this network (HTTP/2 stream cancel);
  build uses the local mirror via `-DFETCHCONTENT_SOURCE_DIR_CATCH2=C:/deps/catch2-src`
  (same override as sibling 11.0 worktrees).

## OUT_OF_SCOPE fix (required for any local evidence)

- master@a5b11b7f does not compile on Windows/MSVC: `src/workflow/pipeline_run_coordinator.cpp`
  `fsyncFile()` uses `_O_WRONLY/_O_BINARY` but its Q_OS_WIN branch includes only
  <io.h>/<windows.h> (the constants live in <fcntl.h>). The sibling open PR
  #1009 worktree carries the identical one-line fix locally. Applied here as a
  minimal one-line include addition so the track can build/test at all
  (autonomy default 6: recorded, minimal, no functional change).
- Second master breakage, same class: `src/agent/data_platform_tools.cpp`
  uses `sicnu::experiment::BenchmarkService` with no `using namespace
  sicnu::experiment;` (D19 landed without it; MSVC rejects unqualified
  lookup). Fixed locally with the one using-directive — the identical fix the
  execution-runtime track shipped as 23b326a4 "fix(agent): master
  build-unblock — qualify D19 BenchmarkService (P0, out of scope)". Both
  one-liners are flagged at the top of PR_BODY for reviewers.

## Resource log
- Build host: Windows (win32, Git Bash). CMAKE_BUILD_PARALLEL_LEVEL=2, CTEST_PARALLEL_LEVEL=1, ninja -j2. CPU/RSS sampling: see PERFORMANCE.md.

## Targeted matrix results (verification run 1, 2026-09-16)

| suite | result |
|---|---|
| test_lab_data_pack | All tests passed (164 assertions) |
| test_lab_grader_kernels | All tests passed (113 assertions) |
| test_lab_grading | All tests passed (542 assertions, 12 cases — corpus incl. labs 8-11) |
| test_lab_batch (baseline) | All tests passed (49 assertions) |
| test_lab_batch_v2 | All tests passed (95 assertions) |
| test_lab_report (baseline) | All tests passed (339 assertions) |
| test_lab_report_cli | All tests passed (62 assertions) |
| test_harness_lab_injection | All tests passed (93 assertions) |
| test_lab_self_check | All tests passed (95 assertions) |
| test_lab_offline_e2e | All tests passed (78 assertions) |
| test_lab_scale | All tests passed (1665 assertions); 1000-run: All tests passed (9819 assertions) |
| test_harness_lab_evals (baseline) | All tests passed (561 assertions) |
| test_labspec | 3/5 pass; 2 PRE-EXISTING failures (see below) |

## Pre-existing master failures (out of scope, recorded)

1. test_labspec "Shipped lab inventory loads cleanly": `data/labs/lab8_temporal_analysis.lab.json`
   (D16) declares id `temporal_phenology_timeline`, which fails D18's stricter
   `^lab[0-9]{2}_...` id check in tests/test_labspec.cpp. Both files untouched by this
   branch (`git diff origin/master -- <files>` is empty); renaming the id changes the
   D16 GUI catalog — other tracks' territory.
2. test_labspec docs-drift probe: `scripts/gen_lab_docs.py` crashes with
   `TypeError: string indices must be integers` on the same D16 lab file (prerequisites
   as plain strings vs the dict format the D2-era generator expects). Same pre-existing
   mismatch; probe also hardcodes `python3`, which does not exist on this host
   (anaconda provides `python`) — an additional host limitation.
3. Determinism/oracle note: the fixtures committed for labspec grading were regenerated
   WITHOUT PROJ data in the generator's first run, producing CRS-less rasters (caught by
   the corpus contract). The generator now hard-fails without PROJ and the committed
   fixtures carry EPSG:4326 (regeneration re-verified with GDAL_DATA/PROJ_LIB set).

## Phase 8 double verification (final)

Two back-to-back full-matrix runs after the review remediation and final
rebase — ALL GREEN both times:

run 1: data_pack 164 / grader_kernels 131 / grading 542 / batch 49 /
batch_v2 95 / report 339 / report_cli 62 / injection 93 / self_check 95 /
offline_e2e 78 / scale 1665 / evals 561 — all "All tests passed".
run 2: identical, all "All tests passed".

Hygiene gates: git diff --check clean; no conflict markers; secret scan
clean; gen_lab_packs.py --check zero-diff. 1000-submission scale evidence:
green (9819 assertions). Total track assertion count per full run: 3974.
