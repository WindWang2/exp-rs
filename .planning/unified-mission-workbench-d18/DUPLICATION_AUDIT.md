# DUPLICATION_AUDIT

Primary findings (see AUDIT_D14_D17):

1. Dual `WorkflowDefinition` name (Engine 2.0 vs IR 2.0) — rename deferred (D-W1).
2. Dual pipeline canvases (`app/workflow` vs `app/pipeline`).
3. Dual guided widgets; dual georef / classify UIs (classic mounted, D14/D15 widgets compiled but unmounted).
4. Dual run coordinators (intentional designer-local vs production).

No deletions in the first slice.
