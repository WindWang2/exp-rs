# DECISIONS — autonomy defaults applied

| # | Decision | Rationale |
|---|---|---|
| D-001 | Track branch based on **current `origin/master` = `27b9aa0a63`**, not the GOAL-pinned `efc5c52f`. | PR runbook step 1 says `-b … origin/master`; origin/master moved 23 commits (~2.6k insertions) ahead of the pin. Autonomy default #10 says review current master. Commits `efc5c52f..27b9aa0a63` (PRs #936–#945 fixes + ExecutionId feature) get an explicit **delta-pass** review recorded in the ledger under `delta/`. |
| D-002 | The two named dossier files (`AUDIT_DOSSIER_ISSUES_747_760.md`, `PROJECT_REVIEW_DOSSIER_5.0.md`) do **not exist** in the checkout (untracked historical artifacts, confirmed by `.planning/professional-workbench-7/BASELINE.md:17` and `dataset-experiment-7/BASELINE.md:14`). Dedupe baseline reconstructed from: full `gh issue list` (#595–#945, 250 issues, archived at `DEDUPE_BASELINE_ISSUES.txt`), `.scratch/audit-final/{findings,issue_dedupe,final_summary}.md` (F-001..F-023, F-101..F-106), `.scratch/findings-audit.md`, `PROJECT.md` (#773–#817 contract table), `.agents/ORIGINAL_REQUEST.md` (#848–#882 batch). | Zero-duplicate gate is enforceable against these; coverage is superset of the two named dossiers (they fed the #747–#760 and #883–#919 issue batches which are in the archived list). |
| D-003 | Skills: loaded on demand, not all pre-loaded. GOAL doc itself encodes the method (six lenses, finding format, severity vocabulary, dedupe protocol). Loading all 8 skills upfront costs context without changing behavior. | Autonomy default "take the default, record it". |
| D-004 | Review unit = file; all Tier A files are actually opened and read. Depth allocation: high-risk classes (numerics, raw pointers, threads, file IO, schema) get full line-by-line; low-risk leaf files (pure UI boilerplate, generated headers) get structural read + lens check. All recorded `reviewed` with honest per-file finding_count. | Completion gate requires 100% Tier A `reviewed`, no `skipped`; honest depth allocation is the only feasible way at 576k lines. |
| D-005 | Verification builds: only when a P0/P1 claim needs execution evidence; target-scoped `ninja -j2`, `QT_QPA_PLATFORM=offscreen`, `ctest -R <name> -j1`. Otherwise findings carry `verified-by-test-draft` / `static-only` honestly. | Build-resources hard rule; avoids 3M-line rebuilds. |
| D-006 | No `gh issue create`; issue drafts under `review/issues/<id>.md`. PR created but never merged (runbook step 8). | Binding envelope. |
