# BASELINE — Phase 0 audit (worktree @ 27b9aa0a63)

## Verified facts (main-agent firsthand)

- 111 distinct `rs:*` operator id strings in src/operators/rs/*.cpp (`grep -ohE '"rs:[a-z0-9_]+"' | sort -u | wc -l` = 111). Matches GOAL.
- Existing labs: 7 (docs/labs/lab1..7), GUI-menu-style, 中文, format 目的/数据/步骤/思考题/预期结果. Sample data in data/samples/ generated by tools/generate_sample_data.cpp (GeoTIFF 256×256, GTiff driver, geotransform+projection set).
- Runner: `sicnu_geo_rs_cli` (src/cli/, target in src/cli/CMakeLists.txt). Flags: `--pipeline/-p <file>`, `--list/-l` (prints `RSOperatorRegistry::instance().operatorNames()` — this is the completion-gate resolution check), `--schema <op>`, `--export-catalog <dir>`, `--resume`, `--experiment-record`, `--no-execution-cache`.
- Pipeline JSON (data/schemas/pipeline_schema.json): object with `steps[]`; step = {id, kind:0, operator:"rs:x", params{}, inputs[{fromStepId,fromPort,toPort}]}. `$stepId.output` placeholder wiring; sequential steps parent-gated automatically (example in rs_pipeline_runner.h:25-35: params.input="$s1.output").
- Temporal input contract (src/operators/rs/rs_temporal_collection_input.h): every temporal operator accepts `scenes`:
  - `scenes:[{path, time:"2025-04-03", bands:{nir:4,red:3}, quality_band, mask_band, modality, polarizations, band_roles}]`, or bare paths + parallel `times` + global `bands`;
  - preflight science gate rejects invalid times / missing roles with typed errors (never silent) → good teaching material for honest-scope labs.
- Temporal family files: temporal_{summary, composite, index(_series? see subagent), trend, anomaly, smooth, gap_fill, harmonic_fit, phenology, sen_trend, monitor, breakpoints, decompose, extract_series, collection_input, output}.
- SAR family files: sar_{backscatter, calibrate, change, dualpol, geocode, ratio, speckle, temporal_stats, terrain_correction, terrain_flatten, terrain_masks, texture} + topographic_correction.
- Hyperspectral: mnf, endmember_extraction, sam_classify, spectral_unmixing, rx_anomaly, continuum_removal, spectral_derivative, spectral_resample, spectral_index operators.
- GDAL Python 3.13.3 available → committed fixture generator will be `scripts/gen_lab_fixtures.py` (deterministic, writes only data/labs/_tmp/, gitignored).
- main/build is Ninja+Release+ccache (launcher /usr/bin/ccache); 16 cores/62GB. Build repro: .planning/lab-content-expansion/logs/build.sh (CMAKE_BUILD_PARALLEL_LEVEL=2, ninja -j2, resource log every 60s).

## Decisions
See DECISIONS.md D001–D005 (base @ 27b9aa0a63; labspec.schema.v1 authored by D3 pending D2; ≤2 concurrent read-only subagents; 中文 docs; committed generator + gitignored _tmp).

## Subagent research
(TEMPORAL+SAR and HYPERSPECTRAL+CARTOGRAPHY reports — appended below on arrival)

## Subagent report A — temporal + SAR (Phase 0, read-only)
Key params verified against source (file:line in the full report, archived in REVIEW materials below):

Temporal (all take `scenes`/`collection`, common `tile_size=256`, `apply_qa_masking=true`):
- rs:temporal_index_series: index enum{NDVI,EVI,SAVI,NDWI,NDBI,MNDWI,NBR,NDRE,NDSI,NDTI}, bands{role:n}; out: 1 band/date + SICNU_ACQUISITION_DATE metadata.
- rs:temporal_trend: out slope(per DAY), intercept, r2, n, rmse; min 2 scenes; R²=1.0 for flat series.
- rs:temporal_phenology: crossingFraction 0.2(0.05-0.95); out sos,pos,eos(DOY),los,amplitude,base,integral.
- rs:temporal_anomaly: method{zscore,difference}, min_observations≥2; out anomaly, baseline_mean, baseline_n.
- rs:temporal_smooth: method{savitzky_golay,whittaker,moving_average}, window 7, degree 2, lambda 10.
- rs:temporal_gap_fill: {linear,nearest}, max_gap_days 90; cannot create dates.
- rs:temporal_sen_trend: alpha 0.05; out slope, intercept, z, p_value, n.
- rs:temporal_summary: out count, valid_count, mean, min, max, stddev (+median).
- rs:temporal_monitor REQUIRES workspace collection UUID — excluded from labs (gap).

SAR:
- rs:sar_calibrate: calibrationA>0 (σ0=DN²/A²), noiseLinear, outputDomain{linear_power,db}.
- rs:sar_speckle: method{lee,enhanced_lee,frost,kuan,gamma_map,refined_lee,multitemporal}, kernelSize odd 3-15, noiseVariance .25, dampingFactor 1.0, looks, companionScenes[]; NoData=NaN; #803 per-band sentinel fixed (run 191-213); warns on dB-looking input.
- rs:sar_change: inputA/B, inputDomain{linear_power,db} (declared-dB with default REFUSES = typed refusal teaching), thresholdMethod{manual,otsu,percentile,statistical}, cleanup, minAreaPixels; out UInt8 mask + stats thresholdUsed/changedPixels/evaluatedPixels/changedPercent.
- rs:sar_terrain_correction: #785 look azimuth fix — lookDirection{left,right} with headingDeg (look azimuth = heading±90), lookAzimuthDeg explicit override ("UIs must not auto-fill"), incidenceDeg 30, flagIncidence.
- rs:sar_temporal_stats: inputs[] N≥2, strict co-registration; 11 bands; argmax_date is 0-based scene INDEX not calendar date (honest-scope note).

Pipeline runner: sicnu_geo_rs_cli; success line "Pipeline succeeded (N steps)", exit 0/1; --json/--json-lines/--quiet; example pipelines in data/pipelines/*.json (11, none temporal/SAR). Placeholder grammar $stepId.port / ${stepId.port} (src/workflow/placeholder_grammar.h:12-20).

GAPS for honest-scope documentation: no filename-date inference; no CCDC/BFAST; gap_fill creates no dates; monitor needs collection UUID; multitemporal speckle uses companionScenes not collection; SAR change strictly bi-temporal; no polarimetric decomposition (H/A/α, Freeman-Durden); topographic_correction optical-only; multi-date σ0 harmonization must run per scene via sar_calibrate.

## Subagent report B — hyperspectral + cartography (Phase 0, read-only)
- rs:mnf: input/output/numComponents(0=all); ≥2 bands; no wavelength metadata needed; GeoTIFF + JSON summary.
- rs:endmember_extraction: input, nEndmembers REQUIRED ≥1, projections 1000 (≥16); deterministic RNG mt19937(42); output JSON {endmembers, indices, ppiCounts} — NO raster.
- rs:sam_classify: input/output/refs(exact bandCount floats each), bands subset, metric{sam,sid}, angleOut optional; SID needs positive spectra; labels 0..n-1, nodata -9999.
- rs:spectral_unmixing: input/output/endmembers, errorOut; LS + clip[0,1] + unit-sum renorm; nEndmembers ≤ bandCount.
- Spectral library: format "sicnu-spectral-library" v1 JSON (src/processing/algorithms/spectral_library.cpp:49-81); NO built-in library ships; rs:sam_classify takes only inline refs arrays (no libraryPath — gap).
- Cartography: MapSpec current spec_version=5 (mapspec.h:12-22; v-history :32-66); collections map_frames/layers/symbols/legends/north_arrows/scale_bars/titles/labels/charts/colorbars/inset_maps/grids/annotations/source_notes/constraints; 55 templates in data/cartography/templates/.
- compose/preflight/repair/export are agent tools (cartography:compose/export) reachable via GUI binary --mcp mode ONLY — NOT rs: operators, NOT in sicnu_geo_rs_cli. Export formats png/pdf/svg, dpi 72-1200 default 300, atomic+SHA-256.
- test_mapspec = aggregate binary (test_mapspec + test_cartography_{tokens,library,quality,templates,visual} + test_platform5..9); READINESS.md:38 "failed", no recorded cause; likely environment-sensitive visual layer. D10 dependency declared; lab must not depend on pixel goldens.
- GAPS: no pipeline/CLI path for map compose/export (MCP scripting needed); no libraryPath param on sam_classify/unmixing; PPI emits no raster; wavelength-aware matching library-only.

### Lab-design consequences (main agent analysis)
1. D3c science-correct chain: MNF for dimensionality/denoise product; PPI + SAM/SID + unmixing all in the SAME reflectance band space (platform has no MNF back-projection operator — mixing MNF-space endmembers with reflectance-space SAM would be scientifically wrong). Typed note in lab.
2. D3d headless evidence plan: runner pipeline produces thematic raster; MapSpec compile/export exercised by NEW lab-chain C++ test (offscreen QgsLayoutExporter) + optional MCP script; dependency on D10 for global test_mapspec redness declared.
3. D3a: trend slope is per DAY — synthetic dates must span a real growing season for meaningful slopes; phenology needs ≥3 valid obs/season.
