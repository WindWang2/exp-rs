# DECISIONS — lab-content-expansion

| # | Date | Decision | Rationale | Alternatives rejected |
|---|------|----------|-----------|----------------------|
| D001 | 2026-09-12 | Base branch on `origin/master` @ `27b9aa0a63`, not GOAL-cited `efc5c52f` | Master advanced 23 commits past the snapshot (verified: `git rev-parse origin/master`). Runbook step 1 says `origin/master`; D5 sibling track made the same call (its DECISIONS.md D001). | Pinning to `efc5c52f` would diverge from runbook step 3 (`rebase origin/master`) and from sibling tracks. |
| D002 | 2026-09-12 | D2 has NOT merged; `labspec.schema.json` absent from origin/master and from D2's local branch (`zcode/lab-spec-data-driven` = `27b9aa0a63`, no commits). Per Autonomy default 7 fallback, D3 defines a minimal `labspec.schema.v1` (JSON Schema draft-07) in `data/labs/labspec.schema.json`, clearly marked as *pending D2 alignment*; this track's 4 LabSpecs validate against it. When D2 merges, its schema wins and these specs get migrated. | Default 7's own fallback ("implement against the schema text committed in D2's branch") is unavailable — D2's branch has no schema text. Blocking is not an option (unattended epic). | Waiting for D2; inventing a richer schema (YAGNI). |
| D003 | 2026-09-12 | "Subagents ≤2" read as ≤2 **concurrent**, read-only, non-nesting — reused across phases (2 in Phase 0 research, 2 in Phase 6 review). | The GOAL itself assigns subagents to both Phase 0-style research ("最多 2 个只读子代理并行调研") and Phase 6 (2 review agents); a global lifetime cap of 2 would contradict Phase 6. | Global lifetime cap of 2. |
| D004 | 2026-09-12 | LabSpec content language: 中文 body (目标/原理/步骤/思考题/预期结果) with English `operator_id`s and schema keys; pipeline JSON keys English. | Matches existing `docs/labs/lab1..7` style and G package requirement (中文撰写). | English-only docs. |
| D005 | 2026-09-12 | Synthetic fixture generation for local verification lives in `scripts/` (committed, deterministic, tiny) writing only into gitignored `data/labs/_tmp/` at run time. | Completion gate allows `scripts/` additions; keeps generated rasters out of git while making headless runs reproducible by reviewers. | Committing fixtures (forbidden); regenerating by hand (unreproducible). |
