# OWNERSHIP — flash-data-scale-13

## Allowed to change (core)

- `src/data/**` — DataManager catalog (snapshot representation, path index), GovernanceStore
  pagination/schema indexes, governance types (additive fields only).
- `src/app/panels/workspace_browser_panel.{h,cpp}` — ONLY the pagination consumer wiring
  (offset→cursor in `fetchMore`/`canFetchMore`); no UI cosmetics, no layout, no styling.
- `tests/**` — new data-scale test target(s) and data perf-test increments; append-only
  registrations in `tests/CMakeLists.txt` (tail-append convention; main agent integrates).
- `benchmarks/observatory/**` — regenerated by the same verify command after the fixes.
- `docs/adr/**` (new data-scale ADR), `.planning/flash-data-scale-13/**`,
  `.goal-loop-ledger.md` (tracked ledger entry).

## Forbidden (parallel domains — do not touch)

- Geospatial range cache / fabric: `src/geospatial/**` (owned by #1137).
- Workflow engine / TaskCenter: `src/workflow/**`, `src/processing/framework/task_center.*`
  (owned by merged #1132/#1130 lanes; observatory workloads there must keep passing unchanged).
- Model runtime: `src/operators/runtime/**`, `src/operators/model_catalog.*`.
- UI cosmetics, themes, QSS, dialogs beyond the panel pagination wiring above.
- Labs/classroom surfaces (#1136).
- No benchmark-gate threshold loosening to hide a regression (gates may only tighten or stay).

## Shared conflict hotspots (coordinate via tail-append / last-writer-wins)

| File | Convention |
|---|---|
| `tests/CMakeLists.txt` | append new target registration at the tail only |
| `.gitignore` | append this track's `.planning` whitelist lines at the tail |
| `.goal-loop-ledger.md` | append this track's ledger section at the tail |
| `benchmarks/observatory/*.json`, `benchmarks/perf-observatory-baseline.md` | derived artifacts regenerated by `perf_observatory_verify`; #1135 may also regenerate them |
| `src/data/governance/governance_types.h` | additive fields only (cursor/nextCursor); no removals/renames |
