# DOCS_LEDGER
Every docs/help/catalog update made by this track is appended below.
- docs/processing/sar-domain.md §4: rs:sar_geocode authority (grid contract, products, real-geometry classes, honesty rule, memory/determinism, evidence).
- docs/processing/sar-domain.md §5: rs:sar_temporal_stats authority (domain rule, valid sample, robust change semantics, products, evidence).
- docs/processing/raster-vector.md: NEW authority doc for rs:rasterize / rs:zonal_stats (membership semantics, last-wins, budgets, CSV columns, evidence).
- Operator metadata(): rs:sar_geocode, rs:sar_temporal_stats, rs:rasterize, rs:zonal_stats carry prerequisites/limitations (surfaced through the meta sidecar + drift tests).
