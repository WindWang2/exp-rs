# ARCHITECTURE — cn-eo-products-sensor-physics-10

See PLAN.md "Architecture (target)" — registry/ImportPlan layout, ImportPlan pipeline
stages, registry schema v1, generation model. ADR (new) will be written in Phase 1 and
linked from here.
