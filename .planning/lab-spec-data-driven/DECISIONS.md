# DECISIONS — LabSpec Data-Driven Labs

| # | Decision | Rationale | Date |
|---|----------|-----------|------|
| 1 | Branch cut from `origin/master` @ `27b9aa0a63`, not goal-cited `efc5c52f` | Goal predates master advance; runbook step 1 says branch off `origin/master`; P8 rebases again. `efc5c52f` verified as ancestor. | 2026-09-12 |
| 2 | Loader is a new file pair `src/app/widgets/lab_spec_loader.{h,cpp}` | Testability: pure Qt Core + jsoncpp, no `main_window.h` dependency; widget cpp can't be compiled into tests without dragging the app shell. Same owner directory (`src/app/widgets/`). Requires 1 mechanical line in `src/app/CMakeLists.txt` + test target sources in `tests/CMakeLists.txt` — the minimal unavoidable diff outside the enumerated dirs. | 2026-09-12 |
| 3 | Steps keep an optional `action` (UI verb slot name) alongside `operator_id`+`params` | Goal default 5 upgrades actionId semantics; load-data / compare / identify steps have no `rs:` operator and must still launch UI affordances. Schema: exactly one of `operator_id`/`action` per step. | 2026-09-12 |
| 4 | LabSpec structural validation hand-rolled in the loader (C++), schema file is the normative contract; repo `schema_validator.h` reused only for params-vs-descriptor drift | Existing `schema_validator.h` validates params against `AlgorithmDescriptor` ports, not arbitrary JSON Schema; no C++ JSON-Schema lib in tree. Generator (Python) may validate with `jsonschema` when available. | 2026-09-12 |
| 5 | Lab files named `labNN_<slug>.lab.json`, zero-padded | Stable lexicographic order across 11 labs; docs regenerate with matching names (`lab1_...` → `lab01_...`). | 2026-09-12 |
| 6 | `grading_ref` optional; points at an existing artifact (e.g. `data/pipelines/*.json`); drift guard resolves it when present | Grading recipes land in D4/D7; contract ships now, no invented dead references. | 2026-09-12 |
| 7 | Base repair `55426a0797`: `mHostProcessRuntime.get()` in `src/plugins/framework/plugin_runtime_host.cpp` | #913 (in master tip `27b9aa0a63`) introduced `auto*` from `unique_ptr` — ill-formed C++, GCC build hard-fails at the plugins framework lib; nothing on this branch can verify without it. Minimal semantics-preserving `.get()`; commit message documents it as a drop-on-upstream-fix repair. Out-of-scope file consciously accepted: a branch cannot verify itself on a broken base. | 2026-09-12 |
